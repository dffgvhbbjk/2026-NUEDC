# 基桩动测仪 - 完整性判别分析报告

## 一、项目概况

本项目为基于 FPGA 的基桩动测仪，用于检测尼龙棒(模拟桩)的完整性。
- **FPGA**: Altera EP4CE6 (Cyclone IV)
- **ADC**: PCM1808, 24位, 95.88kHz 采样率
- **采样周期**: 10.42μs/点
- **纵波速度**: 尼龙棒中约 2000~2600 m/s (实测约 2200m/s)
- **桩长**: ~1000mm

## 二、数据分析结果

### 2.1 数据概况
- 共22个数据组, 约15000+ 次敲击记录
- 每组对应不同的桩状态(好/坏)、缺陷位置(200/305/307/695/800mm)、敲击力度和锤头类型

### 2.2 关键物理参数
| 参数 | 好棒 | d200 | d307 | d695 | d800 |
|------|------|------|------|------|------|
| 基波周期 T0 (采样点) | 90-110, median=99 | 103-110, median=109 | 90-108, median=104 | 100-109, median=108 | 90-105, median=102 |
| r[96]/r[0] | 0.33 | 0.45 | 0.26 | 0.36 | 0.45 |

**规律发现**: 
- 好棒的基波周期 ~96-100 采样点 (≈1000μs, 对应 2×1m/2000m/s)
- 缺陷棒基波周期略微增加 (裂缝降低有效刚度)
- 仅靠 T0 无法可靠区分好/坏 (有大量重叠)

### 2.3 自相关"指纹"特征
每类缺陷在自相关曲线上留下独特"指纹":
- **好棒 (good)**: 自相关光滑衰减, lag 40-60 略有正值, lag 96 出现主峰
- **d200**: 自相关整体很高, 缺乏早期反射特征 (反射太靠近冲击点, 被 SKIP=16 跳过了)
- **d307**: lag 40-70 略有正值, lag 96 主峰但幅值较低
- **d695**: lag 30-50 为负, lag 100-110 出现强峰
- **d800**: lag 40-60 出现**深负区** (关键特征!), lag 100-105 出现主峰

## 三、判别算法

### 3.1 推荐算法: LDA 自相关分类器 (FPGA 已实现)

**算法流程**:
1. 512点帧内找 |x| 峰值位置 imp (主冲击)
2. 有效性检查: peak<300000 或 imp≥112(双击) 或 imp+400>512 → INVALID
3. 自动缩放: s[i] = x[imp+16+i] >> sh, i=0..383
4. 整数自相关: r[k] = Σ s[i]·s[i+k], k=0..135
5. 二次缩放: rs[k] = r[k] >> rsh
6. LDA 打分: score_c = Σ w_c[k]·rs[k] + bias·rs[0] (Q12定点权值)
7. argmax → 类别 (0=good, 1=d200, 2=d307, 3=d695, 4=d800)
8. 800mm二次判别: 若判good但T0≥103 → 改判d800

### 3.2 性能指标 (9000有效样本, 偶训奇测, v2窗口冲击检测)
| 指标 | 数值 |
|------|------|
| 好棒判好 | **95.8%** |
| 坏棒判坏 | **96.1%** |
| 缺陷漏判为好 (关键) | **3.9%** |
| 缺陷定位正确 | **80.6%** |

### 3.3 混淆矩阵 (窗口冲击检测, 新权重)
```
         good  d200  d307  d695  d800   INV
  good    460     0    10     3     7     0
  d200      0   206     0    19    15     0
  d307     61     7   246    27   139     0
  d695      1     5    12   444    18     0
  d800      4    17     1     0   458     0
```

### 3.4 各类详细指标 (v2)
| 类别 | Precision | Recall | F1 | 说明 |
|------|-----------|--------|-----|------|
| good | 0.908 | 0.958 | 0.933 | 优秀 |
| d200 | 0.851 | 0.858 | 0.855 | 大幅改善(原0.159) |
| d307 | 0.950 | 0.512 | 0.666 | 待改进 |
| d695 | 0.959 | 0.925 | 0.942 | 优秀 |
| d800 | 0.737 | 0.954 | 0.831 | 良好 |

### 3.5 关键修复: 窗口冲击检测
**问题**: 旧算法用全局最大峰定位冲击点。对d800铁头桩(组19-20), 缺陷反射峰(距冲击~70点)
可达冲击峰的1.5倍, 导致 `imp >= 112` → INVALID。所有d800铁头帧被丢弃, 显示为"good"。

**修复**: 冲击峰改为在帧前段 `[0, 112)` 窗口内搜索。即使后续反射更强, 也能正确捕获初始锤击。
- 修复前: d800组19 94% INVALID, 组20 64% INVALID
- 修复后: 全部0% INVALID, d800判对率 93.3%

**d200 检测大幅改善**: 旧分析中d200受双脉冲干扰(impact与反射近), 窗口搜索也改善了d200从8.6%→85.8%。

## 四、PC端验证脚本使用

### pile_check.py
```bash
# 批量测试 (仅新批次512点数据)
python pile_check.py --test --limit 100

# 测试单个文件
python pile_check.py --file "God3.0/data/05_xxx/hit_001.csv"

# 从串口读取实时判别 (FPGA数据上行)
python pile_check.py --serial COM3 --baud 115200
```

### analyze_v3.py (深度分析)
```bash
# 完整的逐类自相关特征分析 + LDA训练测试 + 规律总结
python analyze_v3.py
```

### 串口数据格式
FPGA 通过 UART 上行二进数据帧 (与 `wave_uart_export.v` 格式一致):
```
0xAA 0x55 | len_sel | sample[0..N-1] (24位×3字节) | checksum | 结果段(13字节)
```
PC 脚本解析此帧, 独立运行 LDA 分类, 与 FPGA 板载结果对照。

## 五、FPGA 实现要点

### 5.1 已实现模块 (God3.0/rtl/)
- `defect_ac_classifier.v` - LDA自相关分类器 (主算法)
- `defect_distance_calc.v` - 双模式距离计算
- `wave_capture.v` - 512点触发采集状态机
- `wave_uart_export.v` - UART波形+结果导出
- `defect_lda_weights.mem` - LDA权重ROM (1280×16bit, M9K)

### 5.2 资源消耗 (EP4CE6)
- ~30个 M9K BRAM (影子RAM + seg RAM + r RAM + 权重ROM)
- ~1个硬件乘法器 (自相关MAC + 打分MAC 串行复用)
- ~4500 LE
- 单帧分析周期: ~46k 拍 @40MHz ≈ 1.15ms

### 5.3 改进建议
1. **d200 检测**: 考虑使用时域波形直接分析 (缺陷反射在冲击后~17点即到达)
2. **多次投票**: 5击多数投票可提升准确率至 94%
3. **800mm 二次判别**: T0=103 的阈值需要根据最新数据重新标定

## 六、文件说明

| 文件 | 用途 |
|------|------|
| `God3.0/rtl/defect_ac_classifier.v` | FPGA LDA分类器 |
| `God3.0/rtl/defect_distance_calc.v` | FPGA距离计算 |
| `God3.0/rtl/defect_lda_weights.mem` | LDA权重 (自动生成) |
| `God3.0/tools/fpga_model.py` | LDA训练+权重导出脚本 |
| `pile_check.py` | PC端验证脚本 (串口/文件) |
| `analyze_v3.py` | 深度分析脚本 |
