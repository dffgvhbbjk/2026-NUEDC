# Verilog 距离计算算法分析

> 来源：`pc_tools/daima.txt` — `ADC_HUANCUN` 模块（第 940-1150 行）

---

## 一、数据预处理

```verilog
xiugaihou_data <= $signed(vga_wr_ram_data_r1) / $signed(128) + $signed(200);
```

- 原始 ADC 16 位有符号值 ÷ 128 + 200
- 目的：归一化到便于比较的数值范围

---

## 二、斜率方向检测（找波峰/波谷）

```verilog
qiudao_panjue <= (r1 > r2) && (r2 > r3) && (r3 > r4) && (r4 > r5);
```

- 连续 5 个流水线寄存器打拍后的值
- 条件：**5 个连续点严格递减** → `qiudao_panjue = 1`
- 物理含义：波形正在进入**下降段**（从波峰走向波谷）
- 每次 `qiudao_panjue` 翻转（0→1 或 1→0），代表经过了一个波峰或波谷

---

## 三、方向翻转计数

```verilog
qiudao_panjue_edge <= (~q_p && q_p_r1) | (q_p && ~q_p_r1);   // 上升沿 + 下降沿
qiudao_panjue_edge_cnt <= qiudao_panjue_edge_cnt + 1;         // 每次翻转 +1
```

- 分析窗口：`work_cnt` [106, 540]
- 统计波形在这个窗口内总共经历了多少次方向翻转
- 翻转次数越多 → 波形振荡越剧烈

---

## 四、缺陷判定

```verilog
if (qiudao_panjue_edge_cnt >= 11)   // ≥11 次翻转
    quexian_flag <= 1'b1;           // 判为缺陷
```

- 正常好桩：波形较平滑，翻转次数少
- 坏桩（有缺陷）：缺陷反射引入额外波包，翻转次数增多
- 阈值 11 次是经验值

---

## 五、距离计算（核心）

### 5.1 选取测量窗口

```verilog
quexian_check_flag <= (edge_cnt >= 4 && edge_cnt <= 6);
```

在**第 4~6 次翻转**这个区间内进行测量。这个位置对应缺陷反射波包区域。

### 5.2 测量下降沿宽度

```verilog
if (quexian_check_flag & qiudao_panjue)
    ok_length_cnt <= ok_length_cnt + 1;
```

- 当 `qiudao_panjue = 1`（下降段）时，每个时钟周期累加
- `ok_length_cnt` = 该波包下降沿持续了多少个时钟周期

### 5.3 距离公式

```verilog
result_out <= ok_length_cnt * 15;    // 单位: mm
```

```
距离(mm) = 下降沿宽度(周期数) × 15
```

其中 **15** 是综合系数，包含了：
- 时钟周期与 ADC 采样周期的换算
- 纵波在尼龙棒中的波速
- 往返÷2 因子

---

## 六、等效波速反推

若假设 `ok_length_cnt` 的 1 个单位 = 1 个 ADC 采样周期（10.42 μs）：

```
距离(mm) = v(m/s) × N(样点) × 10.42×10⁻⁶(s) / 2 × 1000
         = v × N × 0.00521
         = N × 15              (Verilog 公式)
→ v = 15 / 0.00521 ≈ 2879 m/s
```

若实际波速为 2200 m/s（尼龙棒典型值），则 `sys_clk` 和 `ad_bck` 频率比约为：

```
2879 / 2200 ≈ 1.31   →  sys_clk 频率 ≈ 1.31 × 95.88 kHz ≈ 125.6 kHz
```

---

## 七、结论

| 问题 | 答案 |
|------|------|
| 计算方法 | 检测波形下降沿，测量第 4~6 次翻转处的下降沿持续周期数，×15 得距离 |
| 用的是哪个波峰/波谷 | **第 4~6 次方向翻转**对应缺陷反射波包区域 |
| Web 测距公式 | `距离 = 波速 × Δ样点 × 10.42μs ÷ 2` 原理正确 |
| 系数校准 | 用已知距离的桩（如 d800=800mm）反推等效波速，填入 Web 界面波速框 |
