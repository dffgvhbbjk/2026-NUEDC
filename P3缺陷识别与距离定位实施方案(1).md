# P3 缺陷识别与距离定位实施方案

## 1. 目标

在现有 PCM1808、FIFO、触发采集、波形 RAM 和 VGA 显示均稳定的基础上，实现：

1. 基本要求（3）：自动输出正常/异常标志。
2. 发挥部分（2）：输出缺陷距检测物顶部的距离，单位为 mm。
3. 发挥部分（3）：兼容塑料锤头和金属锤头。
4. 保持现有 64/128/256 点采集、VGA 波形显示和 UART 原始数据导出功能。

比赛对象为约 1m 长、直径 60mm 的尼龙棒。异常棒在中间某处存在断裂。

---

## 2. 官方参考算法结论

官方代码采用两级 RAM：

- 第一级 `ADC_RAM`：连续保存 ADC 数据。
- 第二级 `ram_vga`：保存触发后回读的显示波形。

官方缺陷判断并没有真正定位反射峰，而是：

1. 判断最近 5 点是否连续单调上升；
2. 统计这种状态的上升沿和下降沿数量；
3. 边沿数量达到 11 次时判为异常。

官方距离计算为：

```text
result = 某段单调上升状态持续时间 × 15
```

该方法属于针对样品调出的经验算法，存在以下问题：

- 固定阈值，不能很好适应敲击力度和不同锤头；
- 容易把主冲击回摆当作反射；
- 没有区分缺陷反射和底端反射；
- 距离公式缺少采样率、波速和往返传播关系；
- 噪声或平滑设置变化后，波形单调段数量会改变。

因此本项目只把“波包数量”作为辅助特征，不直接照搬官方判定。

---

## 3. 总体架构

```text
PCM1808 左声道
      |
I2S接收 + 异步FIFO
      |
基线扣除 + 自适应触发
      |
可旁路三点平滑
      |
      +----------------------+
      |                      |
wave_capture             defect_analyzer
      |                      |
512×16波形RAM          缺陷标志/峰位置
      |                      |
VGA波形显示          defect_distance_calc
                             |
                       距离(mm)/置信度
                             |
              +--------------+--------------+
              |              |              |
          VGA字符显示      缺陷标线       蜂鸣器提示
```

`defect_analyzer` 直接监听写入波形 RAM 的同一组有效数据，优先使用平滑链路中的 24 位数据。这样：

- 不占用 VGA 的 RAM 读端口；
- 不增加 RAM 读写仲裁；
- 保留 24 位幅值精度；
- 分析样点与显示样点保持一一对应。

---

## 4. 实测波形带来的算法调整

实测缺陷波形表现为多组宽的正负波包，不是孤立的单样点尖峰。因此不能简单地搜索第一个局部极大值。

应先生成包络：

```text
abs[n] = |sample[n]|

env[n] = (
    abs[n] + abs[n-1] + ... + abs[n-7]
) / 8
```

8 点滑动平均只需要加法、减法和右移，不使用硬件乘法器。

推荐采用滑动和：

```text
sum8[n] = sum8[n-1] + abs[n] - abs[n-8]
env[n]  = sum8[n] >> 3
```

---

## 5. 主冲击波包识别

### 5.1 主峰

从 `smooth_trig` 开始，对随后约 24～32 个有效样点搜索最大包络，得到：

```text
impact_peak
impact_peak_index
```

主冲击的第一个负谷和紧随其后的正峰可能属于同一个波包，不能将正峰误判成缺陷反射。

### 5.2 动态盲区

不使用固定的“触发后跳过 8 点”，改为动态判断主波包结束。

主波包结束条件建议为：

```text
env < impact_peak / 4
```

并连续满足 6～8 个样点。

同时增加最大盲区限制，例如 48 点，避免波形持续振荡导致永远不进入缺陷搜索阶段。

---

## 6. 反射波包检测

主波包结束后进入反射搜索状态。

### 6.1 自适应阈值

```text
reflect_threshold = max(
    MIN_REFLECT_THRESHOLD,
    pretrigger_noise × 6,
    impact_peak / 10
)
```

其中：

- `pretrigger_noise`：触发前样点包络或绝对值的最大值；
- `impact_peak/10`：按本次敲击强度归一化；
- `MIN_REFLECT_THRESHOLD`：防止极静环境下阈值过低。

具体比例需要使用正常棒和缺陷棒原始 ADC 数据标定。

### 6.2 波包合并

单个反射波包内部可能包含多个正负峰。若相邻包络峰间隔小于 10～16 点，则合并为同一个波包。

每个波包记录：

```text
packet_start
packet_end
packet_peak
packet_peak_index
packet_energy
```

第一版可以仅保存峰值和峰位置，不保存全部波包。

### 6.3 有效反射条件

建议同时满足：

1. 包络超过 `reflect_threshold`；
2. 连续超过阈值至少 2～3 点；
3. 与上一个波包间隔达到最小间距；
4. 位于主波包结束之后；
5. 位于正常棒底端反射窗口之前，才作为缺陷候选。

---

## 7. 正常/异常判断

### 7.1 正常棒校准

复位已经迁移至板载 PIN_25，空闲的外接 PIN_53 推荐作为“正常棒校准键”。

校准步骤：

1. 输入实测棒长 `rod_length_mm`，默认可设为 1000mm。
2. 放置正常尼龙棒。
3. 完成一次质量良好的敲击采集。
4. 按下 PIN_53。
5. 保存正常棒底端反射参数：

```text
end_ref_index
end_ref_peak
normal_early_peak
```

校准数据先保存在寄存器中，重新上电后需要再次校准。后续如有需要，再增加 EEPROM/Flash 保存。

### 7.2 判定规则

```text
若在 end_ref_index 之前存在可靠的强反射波包：
    defect_flag = 1
否则：
    defect_flag = 0
```

为了减少误判，建议增加底端窗口：

```text
END_MARGIN = 6～10 点

缺陷搜索范围：
    主波包结束位置 ～ end_ref_index - END_MARGIN
```

可增加辅助判据：

- 缺陷候选峰明显高于正常棒同一区域峰值；
- 波包数量明显多于正常棒；
- 正常底端反射消失或明显减弱。

### 7.3 输出

```verilog
output reg        result_valid;
output reg        defect_flag;
output reg [8:0]  defect_peak_index;
output reg [15:0] defect_peak_value;
output reg [7:0]  confidence;
```

`confidence` 可用以下比例近似：

```text
confidence = defect_peak / reflect_threshold
```

第一版也可以暂不输出置信度。

---

## 8. 缺陷距离计算

### 8.1 推荐：正常棒比例校准

定义：

```text
T0   = 主冲击波包中心位置
Tend = 正常棒底端反射波包中心位置
Tdef = 缺陷反射波包中心位置
L    = 实测棒长，单位 mm
```

则：

```text
distance_mm =
    (Tdef - T0) × L
    ÷ (Tend - T0)
```

该公式能抵消：

- 尼龙材料波速差异；
- 棒长不是精确 1m；
- PCM1808 实际采样率误差；
- 触发和滤波的固定延迟；
- 传感器安装造成的固定延迟。

### 8.2 备用：波速公式

未校准时可以使用：

```text
distance_mm =
    sample_delta × velocity_mps × 1000
    ÷ (2 × sample_rate_hz)
```

当前采样率约 95.88kHz。尼龙实际波速受配方、温度和结构影响，不建议只使用固定波速。

### 8.3 FPGA 定点实现

最大计算量约为：

```text
256 × 1200 = 307200
```

使用 20 位无符号寄存器即可。

新增顺序除法器：

```verilog
module defect_distance_calc (
    input  wire        clk,
    input  wire        rst_n,
    input  wire        start,
    input  wire [8:0]  defect_delta,
    input  wire [8:0]  end_delta,
    input  wire [11:0] rod_length_mm,
    output reg         done,
    output reg  [11:0] distance_mm
);
```

除法使用移位减法，约 20 个 40MHz 时钟完成，对系统实时性没有影响。

---

## 9. 理论距离分辨率

当前采样率约 95.88kHz。若尼龙有效传播速度约 2000～2600m/s，则整数样点对应：

```text
约 10.4～13.6mm/点
```

预计：

- 整数峰定位：约 ±1 点，即约 ±10～14mm；
- 3 点重心或抛物线插值：理论上可达到约 3～7mm；
- 实际误差还受敲击位置、传感器固定方式、裂缝宽度和材料差异影响。

第一版先使用整数样点定位。基本功能稳定后，再增加 1/4 点插值。

---

## 10. VGA 显示方案

新增：

```text
rtl/font_rom_8x16.v
rtl/bin_to_bcd.v
rtl/vga_status_overlay.v
```

不制作完整中文字库，只保存比赛需要的 ASCII 字符：

```text
NORMAL
DEFECT
DIST: 0425 mm
CAL OK
NO CAL
```

建议显示：

- 正常：绿色 `NORMAL`
- 异常：红色 `DEFECT`
- 距离：黄色 `DIST: xxxx mm`
- 未校准：灰色 `NO CAL`
- 校准成功：青色 `CAL OK`
- 在缺陷峰对应的波形横坐标画红色竖线

字符叠加模块输入：

```verilog
input wire [10:0] pixel_x;
input wire [10:0] pixel_y;
input wire [11:0] base_rgb;
input wire        result_valid;
input wire        defect_flag;
input wire        calibrated;
input wire [11:0] distance_mm;
input wire [10:0] defect_screen_x;
```

输出：

```verilog
output reg [11:0] overlay_rgb;
```

---

## 11. 蜂鸣器提示

建议定义：

- 正常：短响 1 次；
- 异常：短响 3 次；
- 校准成功：短响 2 次；
- PIN_54：保留手动蜂鸣器测试。

新增 `buzzer_event_ctrl.v`，对多个蜂鸣器事件统一调度，避免测试键和检测结果互相覆盖。

---

## 12. 推荐按键分配

| 引脚 | 功能 |
|---|---|
| PIN_25 | 板载系统复位 |
| PIN_52 | 清屏/强制重新布防 |
| PIN_53 | 正常棒校准 |
| PIN_54 | 蜂鸣器测试 |
| PIN_55 | 64/128/256 点长度切换 |
| PIN_58 | AUTO/手动增益切换 |
| PIN_59 | 三点平滑开关 |

外接按键均按低有效设计：未按下约 3.3V，按下接 GND。

---

## 13. 计划新增文件

```text
rtl/defect_analyzer.v
rtl/defect_distance_calc.v
rtl/font_rom_8x16.v
rtl/bin_to_bcd.v
rtl/vga_status_overlay.v
rtl/buzzer_event_ctrl.v

sim/tb_defect_analyzer.v
sim/tb_defect_distance_calc.v
sim/tb_vga_status_overlay.v
sim/batch_run_defect.do
```

现有文件预计修改：

```text
rtl/vga_dual_mode_top.v
quartus/vga_oscilloscope.qsf
quartus/vga_oscilloscope.sdc
```

第一阶段不修改现有波形 RAM 结构。

---

## 14. 原始数据采集要求

正式确定阈值前，至少采集：

1. 正常棒 + 塑料锤：3～5次；
2. 缺陷棒 + 塑料锤：3～5次；
3. 正常棒 + 金属锤：3～5次；
4. 缺陷棒 + 金属锤：3～5次；
5. 无敲击环境噪声：至少1组。

每次推荐 256 点、24 位有符号原始数据。

每组数据同时记录：

```text
棒总长(mm)
缺陷距顶部实际距离(mm)
锤头材质
采集长度
平滑开/关
显示增益档位
敲击序号
```

推荐 CSV：

```csv
sample,value
0,120
1,-85
2,42
```

也可以直接保存当前 UART 导出的二进制帧。

---

## 15. 仿真测试计划

### 15.1 缺陷判断

- 只有主冲击，无后续反射：正常；
- 正常底端反射：正常；
- 底端反射前增加强波包：异常；
- 主冲击后立即回摆：不得误判为缺陷；
- 单点噪声尖峰：不得误判；
- 连续多个缺陷反射：报告第一个可靠缺陷；
- 塑料锤低幅值与金属锤高幅值均能判断。

### 15.2 距离

- 缺陷位于 250、500、750mm 的合成数据；
- 棒长为 980、1000、1020mm；
- 除数为零时输出无效，不允许锁死；
- 最大距离不超过棒长；
- 结果误差不超过整数运算预期。

### 15.3 系统回归

必须继续通过：

- PCM1808 I2S 接收；
- 异步 FIFO；
- 触发与重新布防；
- 64/128/256 点采集；
- 平滑旁路；
- 24 位 UART 导出；
- VGA 波形显示；
- HOLD 超时恢复；
- Quartus 全编译及时序分析。

---

## 16. 分阶段实施顺序

### 阶段 A：离线数据分析

1. 收集正常/缺陷原始 ADC 数据；
2. 使用脚本绘制绝对值包络；
3. 确定主波包宽度；
4. 确定缺陷反射和底端反射窗口；
5. 确定阈值比例和最小波包间距。

### 阶段 B：FPGA 缺陷判断

1. 编写 `defect_analyzer.v`；
2. 输出正常/异常和缺陷样点位置；
3. 用仿真和 SignalTap 验证；
4. 暂时通过蜂鸣器或调试信号输出结果。

### 阶段 C：校准与距离

1. 将 PIN_53 设置为校准键；
2. 保存正常棒底端反射位置；
3. 编写定点距离计算模块；
4. 对照游标卡尺/卷尺实测距离。

### 阶段 D：VGA 输出

1. 编写数字和必要字母字模；
2. 显示正常/异常；
3. 显示距离；
4. 在波形上叠加缺陷峰竖线。

### 阶段 E：双锤头标定

1. 分别测试塑料锤和金属锤；
2. 调整归一化阈值；
3. 验证不同敲击力度；
4. 固化最终默认参数。

---

## 17. 验收标准

### 基本要求（3）

- 正常棒连续敲击至少 10 次，无明显误报；
- 缺陷棒连续敲击至少 10 次，能够稳定输出异常；
- 敲击力度变化时仍能正常判断；
- 检测结束后系统能自动重新布防，不永久卡在 HOLD。

### 发挥部分（2）

- 能输出缺陷距顶部的毫米数；
- 输出距离不超过棒总长；
- 重复敲击结果波动建议控制在 ±2 个样点内；
- 目标初版误差：约 ±15～30mm；
- 完成插值和机械安装优化后，争取达到约 ±10mm。

### 发挥部分（3）

- 塑料锤和金属锤均能识别缺陷；
- 不依赖写死的绝对幅值阈值；
- 两种锤头测得的缺陷距离差异控制在可接受范围内。

---

## 18. 开发时注意事项

1. 在拿到正常棒和缺陷棒原始 ADC 数据前，不应最终写死阈值。
2. 不把主冲击后的第一个正峰直接当成缺陷反射。
3. 缺陷检测使用包络波包，而不是单个样点。
4. 距离优先使用正常棒比例校准，而不是固定尼龙波速。
5. 先保证正常/异常稳定，再增加 VGA 字符，避免显示开发干扰核心算法调试。
6. 保留 UART 原始数据导出，便于比赛现场重新标定。
7. SignalTap 至少观察：

```text
impact_peak
impact_peak_index
reflect_threshold
packet_peak
packet_peak_index
end_ref_index
defect_flag
distance_mm
analyzer_state
```

---

## 19. 两端敲差分连续定位法（零训练，真计算路线）

> 本节记录 2026-07 对"真物理计算、非经验档位分类"路线的验证结论。
> 复现脚本：`tools/diag_res_invert3.py`（报告 `tmp/diag_res_invert3.txt`）。
> 数据前提：铁头敲击、96kHz、每锤 256 点原始记录。

### 19.1 核心物理模型

缺陷把全长 L 的棒切成 `d` 与 `L-d` 两段，各自纵向驻波共振
`f = c / (2·段长)`。因此**单端单次测量只能得到无序对 {d, L-d}**，
天然关于 500mm（L/2）镜像对称，无法区分缺陷偏哪一端——这是
"200↔800、307↔695 互相混淆"的物理根源，不是算法缺陷。

### 19.2 关键数据认知：现有数据已含两端敲

- 15/16 组（距一端 307mm）与 11/12 组（距另一端 695mm）**是同一根
  1002mm 棒两端各敲**（307+695=1002）。
- 22 组（200mm）与 19/20 组（800mm）**是同一根 1000mm 棒两端各敲**
  （200+800=1000）。

同一根棒两端敲 = 差分测量：底端反射、结构模态、棒个体差异全部共性，
两次对比只剩缺陷的不对称信息。

### 19.3 两步流程

1. **量距（无序对）**：矩阵束模态估计 + 好棒对比谱 `C(f)=Hd/(0.35+Hg)`，
   谐波加权在短段侧 s∈[100,500] 找峰，得 {s, L-s}。逐棒用自身底端反射
   `dref` 标定波速 `c=2·rodlen/(dref/FS)`。
2. **破对称（定侧）**：⚠️ **未解决 / 已收回**。曾试"高频带 2–5kHz 能量
   占比更低端=近缺陷端"，2026-07-31 用"先算谱后平均"复核推翻——
   **两根棒规律相反**：Rod1 近端 307 谐波少、远端 695 谐波多；
   Rod2 近端 200 谐波多、远端 800 谐波少。此前"4/4 全对"是仅 4 个数据点
   骑在噪声底上的巧合，非物理规律，予以收回。目前**无可靠破对称判据**。

### 19.4 实测复核结论（2 根棒 / 4 端，2026-07-31 修订）

先算功率谱后平均（相位抖动不损失谱能量）的真实谱形：

| 组 | 真值/端 | 基频 | 谐波结构 | 形态 |
|----|---------|------|----------|------|
| good  | —       | 891Hz | 几乎无谐波 | 干净单模 |
| 19/20 | 800 / 远 | 891Hz | 比好棒还干净 | **≈好棒**（缺陷贴棒底、回波并入底反射） |
| 22    | 200 / 近 | 844Hz | 1.7k 中等鼓包 | 多一层中频（肉眼"多一个小波"）|
| 15/16 | 307 / 近 | 656Hz | 1.8k 强鼓包 | |
| 11/12 | 695 / 远 | 797Hz | 1.8k+2.6k 最丰富 | 谐波最多 |

- **用户观察成立**：800≈好棒、200 多一层中频，均由图确认
  （`tmp/spec_compare.png`、`tmp/wave_single.png`）。
- **破对称收回**：谐波多寡与近/远在两根棒上相反，无一致判据。
- **量距**：中段缺陷（307/695）在有序对下 ±10mm；近端 200mm 反演锁到
  333mm（+133mm）——短段 200mm 对应 4285Hz 共振在铁头激励+256点里测不到。

### 19.5 能力边界（诚实版，2026-07-31 再修订：实测脉冲宽 + 缺陷为深锯缝）

**关键物理前提（用户激发）**：缺陷是在 1m 棒上用锯切出的平面，**至少切掉一半
截面**——即**强反射面**（反射系数约 0.33，回波并不弱）。因此“回波弱”不是
缺陷弱，而是**回波埋在发射脉冲里**。

**实测冲击脉冲宽度**（主峰包络降到 10% 的跨度，每组 500 锤一致）：
**69 lag ≈ 720μs → 盲区 ≈ 680mm**（早期误写的 42 lag/375mm 作废）。
对照四个缺陷回波理论位置 `2d/c`（c≈1900）：

| 缺陷 | 回波理论 lag | 落在哪 | 时域可测 |
|------|-------------|--------|--------|
| 200mm | 20 | 深埋脉冲内 | ❌ |
| 307mm | 31 | 深埋脉冲内 | ❌ |
| 695mm | 70 | 正卡脉冲边缘 | ⚠️ |
| 800mm | 81 | 刚出脉冲但并棒底 | ⚠️ |

**统一结论（把本节所有矛盾解释通）**：
- **时域回波定时**：被 680mm 宽盲区堵死，四个缺陷回波全部埋在/压在脉冲
  边缘，回波再强也挖不出——非算法问题。
- **频域腔共振反演**：脉冲结束后（lag>69）顶↔缺陷短腔持续振铃，其
  **周期**反推距离；要求短腔 Q 够高。故 307/695（1.3~3kHz）可测到 ±10~18mm，
  200（4.3kHz，铁锤未激起+衰减快）测不到——频域法下限 ~250–300mm。
- **破对称**：无鲁棒判据（幅度/谐波法均过拟合；回波时序“谁早谁近”方向
  4/4 排序对但逐锤分布噪）。

**唯一物理解锁**：把冲击脉冲变窄 = 换更硬更小的敲击头（钢球/硬质小锤/
更小质量）。脉冲 720μs→~200μs，盲区 680mm→~190mm，200/307 回波直接
露出来，时域定时与频域共振两条路一起变好。软件无法补回。
- 次要杠杆：延长采样窗（>256 点）、补多个缺陷位置的棒做标定验证。
- **样本单薄**：现有 2 根棒仅定性佐证，不构成统计验证。

### 19.6 与档位分类法（LDA）的关系

- 档位分类（`fix_quantize.py`/`defect_freq_classifier.v`）：只能输出 5 个训练
  档位（0/200/307/695/800mm），数据饥渴、任意位置缺陷会被硬贴档位——
  作**兜底**保留。
- 两端敲差分法：零训练、输出连续毫米值，是**符合"真计算"要求的主线**，
  但目前受盲区+样本限制：**仅落在甜区（约 375~850mm）的缺陷可时域定时，
  且尚无可靠破对称判据**。上板前必须先走 19.5 两条硬杠杆（换锤头 / 补棒）。

### 19.7 维纳反卷积突破：好坏判别全线打通（2026-07-31 深挖成果，已全量验证）

**方法（用户"从数学角度想"激发）**：测量信号 x = s∗h（锤脉冲卷积棒冲激响
应）。用好棒做参考通道，复数谱维纳反卷积把锤脉冲**除掉**：

```
Hrel(f) = X(f)·conj(T(f)) / (|T(f)|² + eps·max|T|²)    eps=0.1
h(t) = ifft(Hrel)；判据 amp = max|h[12..110]| / max|h[0..8]|
```

模板 T = 好棒07 偶数锤平均（Hanning 窗，TL=220，PAD=1024，96kHz）。

**判据表现（剔除 good08 脏窗后，逐锤全量）**：

| 组 | amp p25/p50/p75 | 阈值0.70判定 |
|------|----------------|--------------|
| good07/08 | 0.37/0.45/0.55 (p95≈0.65~0.68) | 误报 **~1%** |
| 200mm | 0.67/0.75/0.87 | 检出 ~64% |
| 307mm | 0.93/1.40/1.61 | 检出 ~78% |
| 695mm | 1.41/1.66/1.84 | 检出 ~78% |
| **800mm** | 0.76/0.87/0.92 | **检出 ~75~80%**（旧死结打通）|

单判据 `amp>0.70`（5 锤中位投票更稳）覆盖**全部四个缺陷位含 800**，
之前残差能量判据 800 无效的结论系被脏数据顶高门槛所致。

**good08 脏数据窗（永久剔除）**：13:22:43~13:25:01 约 300 锤
（hit_434~742 时段）。证据三重：反卷积 amp 与残差 rms 两个独立指标
**同一时间窗**同时爆表（0.45→1.38 / 0.15→0.55）、前后自发恢复、时间连续
聚团——判定为采集事故（挪棒/耦合改变），非好棒天生长尾。剔除后
good08 p95=0.65 与 good07 p95=0.68 完全一致。

**方法论沉淀**：
- **两个独立判据同窗异常 = 采集事故**，可正当剔除；单判据长尾不许剔。
- 赛前采集规范：换棒/挪棒后前几锤丢弃；每 50 锤查指标中位，跳变即重采。
- FPGA 在线自检：好棒基线 amp 中位 ~0.45，滑动中位持续 >0.8 报"耦合异常"。

**反卷积揭示的距离信息与镜像简并**：
- 反卷积峰位=缺陷短腔往返周期=**缺陷到最近棒端的距离** min(d, L−d)：
  307→28[26~29] lag≈306mm ✓，695→29[27~29]≈306mm（=1000−695 的镜像）✓。
- 即单端敲击波形中数学上只含 min(d, L−d)，307/695 简并；消歧需借助
  时域回波（695 有 62~63 lag 直达回波，307 没有）或两端敲。
- **消歧判据已定量验证**（PRE=16 坐标下回波带 [67,73]，5 锤票=中位落带且
  IQR≤4）：695 票通过率 **69%**（单锤 IQR=2），307 仅 16%（IQR=19），
  好棒/200/800 全部 **0%**。规则：多锤全体 lag IQR≤4 且中位落带→缺陷在
  远端（直接读出 d）；否则取近端 d=min。测距精度：307 真值307→测306，
  695 镜像真值305→测306，**误差 ±2mm**。
- 200/800（短腔 200mm→5.2kHz）被尼龙高频阻尼吃掉，反卷积也挖不出。

**两层组合系统验证（物理链+频域档位兜底，2026-07-31）**：
- 频谱凹槽家族可分：200/800 家族凹槽在 2.0kHz 与 4.0kHz，307/695 家族在
  1.2kHz；加反卷积峰（~306mm）可定家族，四位置档位识别成立。
- **残留死角→已破局大半：200↔800 镜像方向**（同棒两端，短腔同为 200mm）：
  全窗频谱判别仅 69~72%（早期高频被后 2/3 光滑大波稀释）。用户肉眼看图
  （tmp/cmp_200_800.png）发现真判据：**近端缺陷在冲击后 1.1ms 内有细碎
  高频毛刺，远端光滑**。定量：fe=log10(E[2.5-9k]/E[0.2-2.5k]) @早期窗
  110 点；排序 307(-0.03)>200(-0.53)>695(-0.70)>800(-1.31)>好棒(-1.37)，
  与缺陷距敲击端距离严格单调。fe 与锤力强相关（r≈+0.5，good 斜率
  k=1.80/decade），须力归一 fz=fe-1.80×(log10(pk)-6.7)；阈 fz>-0.91 判近端。
  20 锤投票：200 正确 88%、800 正确 81~88%。局限：仅 1 根棒证据；695 因
  镜像回波（0.65ms）落在早期窗内 fz 偏高，此判据仅限 200/800 家族内用
  （307/695 由物理链反卷积峰直接测距，不走此判据）。另 2.4~2.8kHz 带能
  量近端>远端（+0.51 vs +0.29）可作第二判据交叉。赛前补棒验证后即可用。

**P32 峰周期判据：仅定性物理图景成立，绝对阈值判别已被全量盲跑证伪
（2026-07-31 全家福看图发现 → 端到端盲跑更正）**：
- 定义：P32 = 第三大峰与第二大峰的 lag 差（找窗 P2∈[100,180]、P3∈[190,290]，
  相对冲击峰，抛物线亚采样）。
- ⚠️ **更正（撤回原“阈 107.9、单锤 99~100%、投票全 100%、零判错”结论）**：
  该 100% 是在**手挑的好棒偶数锤干净子集**上测得；把全部原始帧
  （每组约 500~2000 锤，含弱锤/杂锤）喂进 pipeline_defect.py 盲跑后，
  P32 绝对位置严重漂移——**同一根 307mm 棒的 g15/g16 两组，P32 中位
  分别为 106.6 与 169.2（差 60）**，根因是弱锤下 P2/P3 峰被二次峰抢占。
  故**绝对阈值 107.9 不可用于生产**；它导致盲跑中 307 组经家族分支
  乱跳为 200/800。
- 仍成立的是**定性物理图景（延迟记账法）**：裂缝=柔性缝（弹簧），波每
  穿越一次额外群延迟 τ。近端缺陷延迟逐圈累积（dP3≈2×dP2，干净子集
  200: +6.8→+13.3，比值1.96）；远端缺陷缝回波与棒底回波叠形，比值<1
  （800: 0.56~0.74）。此图景解释了“近端缺陷波形早期有毛刺、远端光滑”，
  但 dP3/dP2 比值同样只在干净锤上稳定，未过全量盲跑，**不得作为量产判据**。
- 诚实注脚：P32 测的是**裂缝柔度 τ（缝属性），不是距离**，天生成不了
  连续测距尺；射线追踪正向模型 v1（固定比例反射面假设）连好棒都复现
  失败，需频率相关柔性缝模型。样本仍仅 1 根 200/800 棒。

**波速实测标定（取代拍的 1900）**：c≈**2100 m/s**（695 甜区回波 62~63 lag
反推 2117；好棒棒底回波 94 lag 反推 2043，两独立点交叉验证）。

**其余同批验证结论**：
- 5kHz 低通 FIR 对回波定时**中性**（695 位置 63[62~65] 滤前后不变）、对判别
  与抗噪有益，上板安全；**高通/带通**会产生振铃假峰并毁掉 695 定位，禁用于测距。
- 归一化相关 |cc|/局部RMS 会在安静区饱和产假峰（好棒也冒 1.00"缺陷峰"），
  作废；回波定时一律用幅度域 |correlate| argmax。
- 数据为满 24 位有符号（量化步长 GCD=1，峰值占 19~20 位，SNR 60~80dB），
  已排除“精度不足埋没回波”；FPGA 检测链应保留 ≥20 位，勿过早截 16 位。

### 19.8 端到端缺陷计算流水线盲跑实测（2026-07-31 pipeline_defect.py）

把上述各判据组装成 20 锤/批的端到端流水线（输入原始 ADC → 输出
好/坏+距离），**喂全量原始帧盲跑对账**（非手挑干净子集）。工具：
tools/pipeline_defect.py，报告 tmp/pipeline_v4.txt。

**实测战绩（390 批）：±15mm 正确 40%、±25mm 正确 59%**。分组：

| 组 | 真值 | ±15mm | ±25mm | 距离误差中位 | 状态 |
|----|------|-------|-------|----------|------|
| 07/08 | 好棒 | 稳判 GOOD | — | — | ✅ 好坏链可靠 |
| 19/20 | 800 | 26/29 | 26/29 | 0 | ✅ 但见下方“写死”铁证 |
| 11/12 | 695 | ~1/116 | ~59/116 | **24mm** | ⚠️ 真测距，偏短 21mm |
| 15/16 | 307 | 25/122 | 40/122 | 107mm | ⚠️ 一半漏到家族分支 |
| 22 | 200 | 1/4 | 1/4 | 600mm | ❌ 多数误判 800 |

**三条必须记录的真相**：
1. **300–700 sweet 分支是真物理计算**：`d = lag×MM`（MM=10.94mm/lag）。
   307→测318、695→镜像674，无查表无固定值；盲跑精度 ±25mm 量级，
   够不到赛题 ±10mm。695 系统性偏短 21mm（lag 中位30→短段328 vs 真值307），
   病因待查：波速 c=2100 偏大 或 亚采样峰有偏置。
2. **❌ 800 是写死的固定值，不是算出来的**：pipeline() 家族分支最后一行
   `d = 200 if median(p32)>107.9 else 800`——直接返回字面常量 800/200，
   输入波形里没有任何数被换算成 800。本质是两档分类器贴标签，
   违反零训练纯物理连续计算原则。物理根因：200/800 短腔 min(d,L-d)=200mm
   回波时延太小/频率太高被尼龙阻尼吃掉，反卷积在 [14,48] 挖不出腔峰
   （lag 塌到 12 裙边），无时延就无法换算距离。**整改方向：测不出连续
   距离时应如实报“近端/远端家族，无连续毫米值”，绝不返回伪造固定数。**
3. **好坏判决与 800/好棒家族识别可靠**（amp 判据：好棒中位0.45 vs 缺陷
   1.2~1.6）；但“连续毫米距离”仅 307/695 中段成立且 ±25mm，200 近端无尺。

**方法论教训**：看图找到的判据（fz 毛刺、P32）必须立即做全量鲁棒性
验证，尤其检查“同一真值的不同组之间是否稳定”；手挑子集的 100% 不等
于可盲跑，不得直接写入结论。

### 19.9 流水线 v5：δ 标定 + 路由放宽 + OR 补漏票 + 家族整改（2026-07-31 联合盲跑 + 全量校验）

在 v4 之上做四项试验并全量对账。工具：tools/pipeline_v5.py（v4 未动）、
tools/pipeline_v5_all.py；报告 tmp/pipeline_v5.txt、tmp/pipeline_v5_all.txt、
否决票诊断 tmp/diag_veto.txt。**标定纪律：E1 阈值只用铁头好棒 07/08，
E3 δ 只用 307 标定棒 15/16，其余组均为纯盲测。**

**铁头 9 组同数据对账（390 批，v4 → v5）**：

| 指标 | v4 | v5 |
|------|----|----|
| 好棒误报批 | 15/119 | 15/119（持平） |
| 缺陷检出批 | 247/271 | **261/271** |
| ±15mm 正确批 | 130/390 (33%) | **263/390 (67%)** |
| ±25mm 正确批 | 203/390 (52%) | **269/390 (69%)** |
| 695 误差中位 | 23mm | **4~5mm** |
| 307(g16) 误差中位 | 20mm | **8mm** |

**[E3] 695 偏短 21mm 病因结案**：用 307 标定棒批级甜区峰对账，实测
29.81 vs 理论 28.07 lag，**反卷积峰存在 +1.74 lag（+19mm）系统性正偏置**，
且 15/16/11/12 四组一致（+0.9~+2.9）。dmin 偏大 → L−dmin 偏短，正好
−19mm ≈ 盲跑的 −21mm。**修法：赛前用自有标定棒实测一次 δ，甜区峰减 δ
再换算毫米**（吸收波速误差与包络峰延迟两种病因，无须区分）。校正后
695 误差中位 23→4mm，307→8mm。

**[E2] 甜区路由放宽**：`iqr<=6 OR 批内 |lag−med|<=3 占比>=50%`。v4 里
307/695 大量批被 iqr 卡到家族分支（g15 11批、g12 26批），放宽后基本全部
进甜区连续测距，是 ±15 翻倍的另一半功劳。放宽未引入好棒新误报。

**[E1] 好坏判决 OR 补漏票（两次踩坑后的正确姿势）**：
- 坑1：辅助特征必须用 np_cache 同款 FPGA 定点速度链（window_frame→
  velocity→V0 对齐→冲击峰归一 200 点）；改用原始加速度域或自写浮点
  积分链，FWHM 完全无区分度（好棒/缺陷全部重叠）。
- 坑2：把辅助特征做成“否决票”方向就错了——诊断（diag_veto.txt）证明
  g08 的 14 个误报批 amp 高达 1.2~1.6 且 FWHM 30~36，**波形真长成缺陷样，
  否决不掉**；而 800 组 FWHM 51~57 长得像好棒，会被否决票误杀。
- 正确设计：**OR 补漏**——`amp>0.70 或 FWHM<39.5 或 高频比>−38.6dB
  任一越阈即 DEFECT`，阈值取干净好棒批（amp≤0.70 的 07/08 批）包络
  min/max 外加裕量。效果：检出 +14 批（捞回 amp 漏掉的 307/200），
  好棒误报不涨。
- g08 有 3~4 批 amp 1.2+ 的“真缺陷样”批任何声学判据都救不了，怀疑该
  场次混入异常敲击，占比 ~4%，在好棒 90% 正确率要求内可容忍。

**[E4] 家族分支整改**：废弃已证伪的 P32，改 fz>−0.91 逐锤投票，只输出
NEAR/FAR 方向，**不再返回写死的 200/800 毫米字面量**。

**全量校验（22 组全上，塑 9 + 中敲 3 + 干扰 1 为盲测域）关键事实**：

| 域 | 好棒判对 | 缺陷检出 | 连续测距 |
|----|---------|---------|---------|
| 铁头 9 组 | 104/119 (87%) | 261/271 (96%) | ±15 = 263/390 |
| 塑料头 9 组 | 93/97 (96%) | **230/349 (66%)** | 基本不可用（甜区极少触发） |
| 中敲 01/03/04 | 旧 256 点格式，v5 无 512 帧可用 → 全 INVALID | | |
| 干扰 02 | 无有效冲击帧 → INVALID（正确） |  | |

1. **好棒判决跨锤头成立**：塑料头好棒 05/06 判对 93/97，amp 判据+补漏
   票对好棒不误伤。
2. **缺陷检出对锤头强依赖**：塑料轻敲大量漏检（g09 漏 18/24、g14 漏
   70/99、g18 漏 20/81）——塑料头激励不出高频缺陷特征，**现场必须铁头**，
   与 §19.7 结论再次互证。
3. **塑料头几乎进不了甜区**（反卷积 lag 塌裙边），连续毫米测距实际上
   是铁头专属能力。
4. **fz 方向票的真实边界（全量修订）**：800 家族 **112/112 全对(FAR)**、
   307 家族 53/56 对(NEAR)；但 **200 家族 1/18（塑 200 g21 0/15 全反）、
   695 漏到家族分支的批 21/69 大面积错**。fz 只能做“800/307 方向确认”，
   **不能当 200 的定向依据**——200 的判向问题依旧未解，仍需两端敲差分
   或补采数据另找判据。

**固化建议（进正式方案）**：δ 标定与 E2 放宽属纯赚不亏，立即固化；
E1 补漏阈值与 E4 方向票边界在补采数据（铁头 200 第二场次）后复核。

### 19.10 v6 两腿融合：甜区连续测距 + 冻结 V2 档位分类兜底（2026-07-31）

§19.9 的“200/800 结构性无尺 0/33”只说了反卷积连续测距这一条腿；
《铁头频域缺陷档位分类方案》（冻结 V2：FIR5k+16维+LDA+投票，留组
96.7~100%）正是给这些批兜底的另一条腿。两腿融合盲跑（tools/
pipeline_v6.py → tmp/pipeline_v6.txt，档位 LDA 严格留一组出训练）：

决策链：E1 好坏 → 甜区通过则连续毫米（物理优先）→ 进不了甜区的用
档位分类报档位值 → GOOD 但档位强多数(≥70%)判缺陷则翻案。

| 指标 | v5 | **v6** |
|------|----|--------|
| ±15mm | 263/390 (67.4%) | **319/390 (81.8%)** |
| ±25mm | 269/390 (69.0%) | **325/390 (83.3%)** |
| 800 档（g19/20） | 0/29 | **28/29（档位票 err=0）** |
| 307（g15/16） | 58/122 | **78/122（漏甜区批由档位捾回）** |
| 695（g11/12） | 101/116 | **109/116** |
| 好棒误报 | 15/119 | 15/119（翻案机制未误伤好棒） |

分支使用：sweet x220（连续物理测距主力）、gear x54（200/800+漏甜区
批兜底）、rescue x5（档位翻案救回漏检）。

仍诚实声明：g22（200 唯一组）留组时无 d200 训练样本，盲跑 0/4（3 批
被判 800）——与冻结方案 §5.4 风险声明一致；赛场实际部署时 d200 有训练
样本（非留组），但只有补采铁头 200 第二场次才能给出可验证成绩。

### 19.11 E5 wave220 丢帧修复：200/800 批数恢复（2026-07-31）

**丢帧黑洞**：`wave220` 用全长 argmax 对齐 220 点维纳窗，而 200/800
铁头锤的最强点常跑到第 ~340 采样（晚期强震铃），窗越界整锤作废：
g22 500→83 锤、g19 500→69、g20 2000→522。批数被压成 4/3/26，gear
腿票源跟着枯竭。数据本身完好（500 锤全部通过冲击峰校验）。

**修法（E5，pipeline_v5.py）**：准入门槛改为定点速度链（FPGA 前端
np_cache 同款）通过即计锤；维纳链降级为可选特征（lag/amp/e 可为
None）。三处守卫：

1. 甜区路由要求批内维纳有效锤 ≥10/20，不足即转家族分支（gear 兜底）；
2. E1 amp 票要求批内维纳有效锤 ≥5/20 才可投 GOOD——维纳大面积作废
   本身就是晚期震铃的缺陷证据（好棒 07/08 维纳有效率近 100%）；
3. 标定环节（07/08 阈值、15/16 δ）同步过滤 None。

维纳有效率普查：好棒/307/695 组 88~100%（几乎不受影响），
800 组仅 14~26%、200 组 17%——丢帧完全集中在塌裙边家族，修复
不扰动甜区腿的既有语义。

**新口径战绩（524 批，留组盲测，敲一次记一次分）**：

| 指标 | v4 | v5 | **v6** |
|------|----|----|--------|
| ±15mm | 131/524 (25%) | 266/524 (50.8%) | **422/524 (80.5%)** |
| ±25mm | 206/524 | 274/524 | **430/524 (82.1%)** |
| 好棒误报 | 19/124 | 19/124 | 19/124 (15.3%) |
| 缺陷检出 | 367/400 | 392/400 | **396/400 (99%)** |

分组变化（对照 §19.10 旧口径）：
- **g19 800：3→25 批，25/25 全对 err=0**；**g20 800：26→100 批，
  99/100，err=0**——800 档从样本饥饿变成最稳档位（124/125）；
- g11/g12 695：24/25 + 95/100（±25 达 25/25 + 100/100）；
- g15/g16 307：12/25 + 62/100，仍是最弱档（轻敲信噪比 + 甜区/档位
  两腿交界），待补采复核；
- **g22 200：4→25 批，仍 0/25**——留组时无 d200 训练样本是数据缺陷
  不是算法缺陷（场次内自证 12/12 全对已单独验证），只有补采铁头 200
  第二场次才能转正；
- 剔除 g22 后 ±15 = 422/499 (**84.6%**)，对比旧口径 319/386 (82.6%)，
  修复为纯赚。

分支使用：sweet x232、gear x165（票源恢复后成为 200/800 主力）、
rescue x4、fb x14。好棒误报率 12.6%→15.3% 微涨系恢复的 5 个 g08
批中 4 批本就是"真缺陷样"批声学（§19.9 已记录），非修复引入。

### 19.12 xfz 方向票交叉验证：g22 留组从 0 到 13/25（2026-07-31）

**考古复盘**：档位方案里"g22 也能过（89.8% @9锤）"的出处是
iron_boost 三类实验（good/near/far，**far 当时只有 695，尚无 800
数据**）；复现确认原配方 g22 批级 23/25 判 near。但 800（g19/20）
一加入类空间成绩即塌（并入 far → 11/25；单列四类 → 17/25）——
200 的谱签名被 800 吸走，正是 v6 五类留组下 g22 判成 800 的病根。

**新钥匙**：E5 新口径下 fz 方向票在塌裙边家族内部方向性极强：
g22 批级 22/25 判 NEAR，g19/20 则 123/125 判 FAR。于是加 [3b]：
档位判 800/good 但批内 fz 强判 NEAR → 改判 200（分支号 xfz）。

**战绩（524 批留组盲测）**：±15 = 433/524 (**82.6%**)、±25 = 441
(84.2%)；**g22：0/25 → 13/25（DEFECT@200 err=0 ×13）**；代价是
g19/g20 各被 xfz 误改 1 批（800 档 124/125 → 122/125），净赚 +11 批。
g22 剩余错批主要是 7 批被判 307（gear）——200 vs 307 同属 NEAR，
fz 方向票无法再拆，仍需补采铁头 200 第二场次才能彻底转正。
风险声明：xfz 规则的设计动机来自分析 g22 的失败模式（阈值本身
未拟合 g22，fz 阈 FZ_FAM=-0.91 沿用 §19.7），严格意义上弱于纯盲。

### 19.13 FPGA 落地进度盘点与 v6 简配上板方案（2026-07-31）

> **⚠️ 本节“档位腿单腿上板”路线已被用户硬规则否决**（板上绝对禁止
> 档位近似输出，距离必须连续物理计算）——见 §19.14 新路线。
> 本节保留作为盘点与数据存档；其中 fz 定点化成果（步骤①）在新
> 路线中继续使用（fz 方向票是连续测距的必需组件）。

**已上板（RTL 现状摸底）**：
- `rtl/defect_freq_classifier.v` 已完整实现冻结 V2 档位分类
  （基线扣除→梯形速度积分→峰对齐定标→FIR5k Q15→w50/w30→hanning
  →Goertzel 13bin→log2 查表→16 维去均值→LDA int16 五类→多锤投票），
  全串行复用一个 33×33 乘法器，全流程 ~33k 拍 @40MHz < 1ms；
- 权重链已闭环：`tools/fix_quantize.py`（RTL 位真定点参考模型）+
  `tools/export_weights.py`（自动生成 `rtl/dfc_rom_init.vh` + 金标）；
- `sim/tb_defect_freq_classifier.v` 45 帧位真回放通过（16 维特征 +
  5 类分数逐位一致），无效敲击/投票/清零用例全过；
- 顶层 `vga_dual_mode_top.v` 已用 defect_freq_classifier 替换
  defect_analyzer（后者已从 QSF 移除），结果接 vga_status_overlay +
  wave_uart_export；
- 全编译 **5850/6272 LE = 93%**（面积激进优化后），M9K 20%、
  乘法器 8/30。注意：档位方案 §6.1 预测替换后 ~55% 系仅按
  defect_analyzer 3719LC 净移除估算，实际分类器+外围占用更高，
  余量仅 ~422 LE。

**不上板（资源判死）**：v5/v6 的维纳反卷积甜区腿（rFFT1024 +
复数除法 + 模板 ROM）在 93% LE 现状下不可行，保留为离线工具链
（UART 导出 CSV 赛后复核）。FPGA 实时判决 = 档位分类腿单腿。

**待上板增量（本轮任务）**：把 §19.12 的 xfz 方向票搬上板——
fz 特征（冲击后 110 点窗 2.5–9k / 0.2–2.5k 频带能量比，力度归一）
可在 raw 镜像 RAM 上用 Goertzel bin 组复用现有串行乘法器实现，
免 FFT；判决层加 [3b]：投票判 800/good 但 fz 票强判 NEAR → 改判
200。

**FPGA 单腿口径盲跑（`tools/pipeline_fpga.py`，524 批留组）**：
定点 dfc（A=16384/FRAC=5/int16 LDA，与板上同源）逐锤判档 +
批内 20 锤多数票，三变体对照：

| 变体 | ±15 | 检出 | 好棒误报 | g22 | 备注 |
|---|---|---|---|---|---|
| A 纯投票（板上现状） | 476/524 (90.8%) | 384/400 | 15/124 | 0/25 | g11/12 全对，g15/16 22+96 |
| B +xfz（仅救 800） | 476 (90.8%) | 384 | 15 | 2/25 | 改判 x4 |
| **C +xfz（800/good）** | **487 (92.9%)** | **398/400** | 15 | **13/25** | 改判 x18，好棒零误触发 |

**采纳变体 C**：g22 的 12 个 vote-GOOD 批全部被 fz NEAR 翻案成
DEFECT@200（漏检→检出），而 g07/g08 好棒的 fz 全判 FAR，xfz 一次
未误触发——fz 同时能拆 “真好棒 vs 漏档 200”。代价：g19/g20 各
误改 1 批（同 §19.12），g15/16 有 3 批错批内部换错法（±15 不变）。

**诚实口径声明**：单腿 90.8/92.9% 高于双腿 v6 的 82.6%，是因为本
数据集真值恰落在档位上（档对即 err=0）；赛场缺陷位置任意时，
档位输出只是最近档近似（档间隔最大 388mm），而维纳甜区腿能测
连续毫米——两者不可比。板上取档位腿是资源限制下的工程拘束，
非算法优劣结论；连续测距保留在离线链（UART 导出复核）。

**上板实施步骤**（进行中）：① fz 定点化（Goertzel 带能量 +
log2 查表 + 整数阈值，验证与浮点 fz 判决一致率）；②
export_weights 增量导出 fz ROM/阈值 + 金标；③ RTL 加 fz 通路与
xfz 投票修正；④ TB 位真对拍；⑤ 全编译（LE 余量 ~422，紧）。

### 19.14 板上连续测距路线：维纳反卷积时域化免 FFT 上板（2026-07-31）

**背景**：用户硬规则——板上输出绝对禁止档位近似（200/307/695/800
固定档）；数据集只是验证方法的工具，方法必须对任意缺陷位置成立。
§19.13 的档位单腿上板方案作废，改走连续测距上板。

**关键突破（`tools/wiener_conv_fix.py`）：维纳反卷积 = 固定滤波器时域卷积**。
K(f)=conj(T)/(S+λ) 由赛前固化模板决定，k=irfft(K) 是固定实系数；
判决只用 e[0..110]，故只需 330 个 int16 ROM 系数 + 串行 MAC
111×220≈24.4k 拍 @40MHz ≈0.6ms——**免 FFT、免复数除法**，复用 dfc
串行乘法器架构即可上板。验证：浮点恒等最大相对误差 6e-16（纯数学
恒等）；定点位真链 lag 一致率 99.97%（7864/7866 锤）、amp 好坏票
一致率 99.99%，|Δlag|>1 仅 1 锤。

**板上全链盲跑（`tools/pipeline_board.py`，524 批，无档位兜底）**：
判决链 = 定点卷积腿 amp 票 + fwhm/hig 好坏判 → 定点甜区路由（批平均
包络整数归一 + 整数抛物线亚采样 Q4 + δ 定点标定）→ 连续毫米
 d=(lag16-δ16)·175>>8；方向由 fz 定点票定（THR_SWEET=-6569）；塌裙边
批 → fb 只报方向 NEAR/FAR（THR_FZ=-6894），**不给伪毫米，±15 诚实记 0**：

| 链 | ±15 | ±25 | 检出 | 好棒误报 | fb 方向票 |
|---|---|---|---|---|---|
| 板上定点 | 263/524 (50.2%) | 270 | 392/400 | 19/124 | 162/178 对 |
| v5 浮点对照 | 266/524 (50.8%) | 274 | 392/400 | 19/124 | 163/178 对 |

定点化仅掉 3 批（±15）——位真可上板。g11/12（695）定点连续测距
err 中位 4～5mm，g16 中位 16mm。低分是口径诚实：数据集 200/800
（275 批）全在裙边只能报方向；甜区为路由窗名义 153～847mm，但
**实证锚点只有 307/695（含镜像 693）两档：真实下限落在 200～307mm
之间，数据不足以精确定位（见 §19.16）**。赛题未限定缺陷位置
（题目图 3/图 4 示意含近端裂缝），≈300～700mm 是本系统实证能力
范围而非赛题承诺；缺陷落在甜区时板上链的真实水平对标的是
g11/12/15/16 的表现（±15 命中 158/250 = 63%，其中 695 组 112/125=90%）。

**裙边捞回证伪（`tools/diag_skirt_rescue.py`）**：假设 200/800 塌 fb 是
晚期震铃锤 wave220 全长 argmax 越窗所致，改冲击峰对齐捞回——结果
所有组 lag 中位压到 12 下边界无信息；对照 v5 普查，有效锤本身在
307/695 组 lag≈29-31 有信息，而 200/800 组即使有效锤也压边界——
**塌裙边是回波与主震荡物理重叠，非对齐 bug；fb 只报方向是诚实上限**。

**上板方案（新）**：在 dfc 基础设施上加两条定点通路：
1. **fz 通路**（§19.13 步骤①已验收，一致率 99.49%）：方向票 +
   好坏辅助，47bin Goertzel 复用串行乘法器；
2. **卷积腿**：k_rom 330×16bit（<1 M9K）+ 串行 MAC + lag 簇聚路由 +
   批平均包络/亚采样/δ 扣除（全整数，已位真验证）；
3. 输出：GOOD / DEFECT@连续毫米 / DEFECT+方向（裙边）；
   LDA 档位腿不再作为距离输出（可保留作好坏/方向辅助票，若 LE
   不够先砍）。LE 风险：余量 ~422，卷积腿控制逻辑需精简；必要时
   用“卷积腿替换 LDA 腿”而非叠加。

### 19.15 裙边（<150mm / >850mm）连续测距三法验证记录（2026-07-31）

用户追问 200/800 能否连续测距，对裙边区三路进攻，全部留实验记录：

**① 时域残差包络腿（`diag_skirt_resid.py` / `diag_resid_batch.py` /
`diag_synth_delta.py`）**：好棒模板包络扣除后找残差峰。重大发现：
800 端信息存在且强聚束（g20 锤中位 lag 15、iqr=1.0；g19/20 批级
err 中位稳定 68mm 系统偏差），好棒伪峰在 28-30 不混淆。但合成注入
自标定（零训练：模板+已知延迟反射扫 lag_m(τ,α) 面）证实：裙边段
斜坡把分辨率压缩 ~4:1（τ 走 15 步 lag_m 只走 4 级），闭环成立
（τ=18→lag_m 15-16 = 实测）但反查表放大噪声 4 倍，盲验 ±25 命中
≈0——**时域裙边分辨率不足，非偏置可校，不可逆**。

**② 谱凹槽反解 d=c/(2Δf)（`diag_notch_skirt.py`）**：批平均去趋势谱
find dip。结果 Δf 全组趋同 ~900Hz（含好棒）——谱梳被系统响应/棒底
模态支配，缺陷凹槽不可提取，**死路**。

**③ 短腔共振峰反解 d=c/(2f₀)（`diag_reson_skirt.py`）**：晚期震铃窗
2-9kHz 找突出峰。结果峰位多模态混杂批间不稳（g22 少数批碰到
~5250 理论值但多数在 2578；g19/20 在 2438/3492/6820 间跳），单
argmax 不构成可靠反解——频域信息够 LDA “分类”但不够“连续反解”。

**物理结论**：冲击脉冲宽 ~110 点（1.15ms ≈ 双程 1.2m），反射体距
两端 <~150mm 时回波与主震荡重叠，信息在时域被压缩、在频域被模态
混叠——**裙边连续毫米反解在本硬件带宽下不成立，三法实验坐实**。
板上诚实输出：甜区连续毫米（实证覆盖见 §19.16）；裙边报方向+物理
区间（NEAR / FAR，由 fz 票定方向，162/178 对）——这是
物理上限的诚实表达，不是档位近似。

### 19.16 甜区真实边界测绘尝试（2026-07-31）——合成法证伪与诚实口径

用户指出"153~847mm 包括 200 和 800"的矛盾：153~847 是 lag 路由窗
[14,48] × 10.94mm 的**名义覆盖**，非实证覆盖。为定位真实下限做了
三轮合成注入测绘，全部失败，但失败方式本身有结论价值：

**① 纯模板自合成（`diag_sweet_bound.py`）**：w=tpl+α·delay(tpl,τ)
直接过维纳。τ 全域（含 28=307mm）lag 恒 12——与实测 g15/16 lag≈31
矛盾。原因：tpl 与维纳核完美匹配，反卷积把主瓣压成 δ 峰后其归一
幅度仍盖过 α≤1 的回波副本。**实验作废**。

**② 真实锤 raw 域单回波注入（`diag_sweet_bound2.py` /
`diag_bound_sanity.py`）**：r_syn=r0+α·shift(r0,τ)，g07 500 锤全链。
τ=14..55 全域 lag 仍恒 12。sanity 对比揭示关键事实：真实 g15（307）
锤的维纳包络 **lag31 回波峰是全局最大（100%），主瓣只有 70%**；而
线性注入回波峰最高只到 0.2~0.5（α=1 也压不过主瓣）。另发现负 α
（自由面极性翻转）时峰位才出现在 lag30/34——极性方向正确但强度
差数倍。

**③ 短腔多次往返模型（`diag_bound3.py`）**：
rs=r0+Σ(-g)^k·shift(r0,kτ)，K=5，g∈[0.5,0.9] 扫描。τ=28 校准全部
lag=12、主瓣高 1.00、corr(g15)≤0.34——**仍无法复现真实形态**。

**结论**：真实裂缝回波不是主脉冲的"延迟缩放副本"——裂缝改变的是
传递函数（频散、模式转换、能量重分配，真实缺陷锤反射能量在维纳
域反超主瓣），线性注入伪造不出来，**合成法测不出甜区边界**。

**诚实口径（写给评测/答辩）**：
- 实证可测锚点：dmin=307（g15/16 直接 + g11/12 镜像 693→307）
  与 305（g11/12 若 L=1002 敲另一端）；实证塌点：dmin=200（g22/19/20）。
- **真实甜区下限落在 200~307mm 之间，现有数据无法进一步细分**；
  上限由镜像对称给出（L-下限）。
- 板上表述：路由窗 lag∈[14,48] 保持不变（不排除 200~307 间某些
  真实缺陷可测——路由本身由 lag 簇聚质量自动裁决，可测就给毫米，
  不可测走 fb 方向腿）；宣称覆盖时用"实证 ≈300~700mm，边界
  200~307mm 之间未标定"，不再宣称 153~847。

### 19.17 "赛题重点区间 300-700"臆断更正与裙边策略重评（2026-07-31）

用户质问"赛题重点区间 300-700 谁说的"——查证后确认**无出处，系
臆断**：旧方案废弃的"300/700 幅值分支"与数据集 307/695 两档被
混同成了"赛题重点区间"。赛题原文（tmp/pdfs/contest/task-2/3.png）
对位置定损只要求"输出缺陷处相对顶部距离(mm)"，**无任何范围限定**，
且图 3/图 4 示意图明确画了上/中/下三种裂缝位置——裙边缺陷是真实
赛场风险。

评测表（evaluation-2.png）位置定损按 Δ 分档计分：Δ≤10 得 5.5，
逐档递减，**Δ≤200 仍得 0.5 分，仅 Δ>200 才 0 分**。由此裙边策略
需重评：
- "只报方向不给毫米" = 该项稳拿 0 分；
- 残差腿（§19.15①）800 端批级 err 中位 68mm、强聚束：若输出连续
  毫米可得 ~2 分/次；它是残差包络峰→lag→mm 的连续物理计算，
  不违反"禁档位近似"硬规则；
- 此前用 ±25 判残差腿死刑是用错了尺子——评分容差到 200mm。
裙边是否改为"残差腿连续毫米兜底"待用户拍板。

### 19.18 ±20mm 目标验收：方向阈修正打满甜区上限（2026-07-31）

用户定目标 **±20mm（Δ≤20 = 5 分档）**，纪律：数据上做到才动 RTL。
±20 尺子下解剖 307 组失败模式（diag_307_fail）：±20 仅 41%，其中
**38% 是"lag 全对、fz 方向票投错"的错端批**（48 批 medL 28-31 簇聚
质量好，输出镜像 693 侧）——瓶颈不是测距是方向。

方向修复过程（全部有脚本+报告）：
- **二次回波谐波判据证伪**（diag_dir_harmonic）：h2r=e[2L]/e[L] 预测
  NEAR 高 FAR 低，实测反向且 g16 与 FAR 重叠，最优阈总正确率 56%≈
  抛硬币，弃。
- **批级 fz 连续量解剖**（diag_dir_fzmed）：批 vmed=median(valq)
  在 NEAR/FAR **几乎完全可分**（g15/16 中位 -6576/-6559 vs g11/12
  -6724/-6827）。病根：旧 THR_SWEET=-6569 恰好压在 NEAR 分布中心，
  逐锤票在半数线乱晃。阈值移进间隙（-6654）：方向 98.1%，甜区批
  ±20 = 206/215 = **96%，打满 oracle 上限**（oracle 也是 206——
  方向全对时 Q4 测距链只丢 9 批）。
- **LOGO 盲验**（diag_dir_logo）：折 A（标 g15+g11 中位 midpoint
  -6650 → 盲测 g16+g12）方向 100%、±20=99%；折 B（小样本 37 批）
  方向 92%、±20=78%；两折合计=206/215 与全扫描持平——**标定流程
  可迁移非偷看**。赛前标定动作：自有棒近/远各做一缺陷，取两端批
  vmed 中位的 midpoint 烧 ROM（THR_DIR）。单边标定（仅 307 棒+good）
  为降级方案：方向 89.8%、±20=86%。逐锤票计数与批中位数等效
  （206=206），RTL 免中位数排序。

pipeline_board 换 THR_DIR=-6650 后 524 批终验：

| 口径 | 板上定点 | v5 浮点对照 |
|---|---|---|
| 甜区组(307/695) ±20 | **206/250 = 82%**（塌批诚实记0） | 169/250 = 68% |
| 甜区路由成功批 ±20 | **206/215 = 96%** | — |
| g16/g11/g12 err 中位 | 6/5/4 mm | 9/6/5 mm |
| 全 524 批 ±20 | 311 | 274 |
| 检出/好棒误报/fb方向 | 392/400、19/124、162/178 | 同级 |

**±20 验收结论：缺陷落在甜区且路由成功时 96% 达标（err 中位 4~6mm）；
定点链全面超过浮点 v5（方向阈修正只进了定点链）。残余损失：g15
弱批塌 fb（25 批中 10 批，轻敲信噪比低）与裙边物理上限（§19.15）。**
方法验证到此收口，满足"做到了才动工"纪律，等待用户批准进 RTL。

### 19.19 RTL 动工第一步：512 点采集链重建（2026-07-31）

用户批准动工后的守门发现（RTL 动工前必跑口径对齐）：

1. **256 点采集窗下卷积腿存活率 = 0（全灭，tmp/surv256.txt）**。
   维纳对齐峰（全帧 argmax 考古口径）不在冲击峰附近而在 230~270
   点晚期共振区，w220 窗需伸到 ~475 点 → 必须 512 点帧。
2. **数据集 512 点 CSV 的来源谜底**：g01（07-29 采）为 256 点，
   g15~g22（07-30 采）为 512 点；采集当天的 512 固件+parser 改动
   未存盘（全仓库零痕迹，git 无提交），据数据反推口径：512 点/
   预触发 64（冲击峰 p0 全部落在 ~80 点）。

重建实施（全部编译干净，ModelSim 全绿）：

| 模块 | 改动 |
|---|---|
| wave_capture.v | len_sel=2'b11→512 点/预触发 64；512 模式整块 RAM 单一循环缓冲（不乒乓），9 位指针回绕；新增 export_busy 输入，导出中锁 HOLD |
| wave_uart_export.v | 帧长加 512（len_sel 字节=3），9 位线性寻址回绕；busy 回接 capture |
| vga_waveform_precision.v | 512 点 1px/点映射，标签 512pts/5.34ms |
| defect_freq_classifier.v | mir_addr 改 9 位线性回绕（暂仍只分析前 256 点，卷积腿改造后用满 512） |
| vga_dual_mode_top.v | len_sel 固定 2'b11；uart_export_busy 回接 |
| uart_frame_parser.py | LEN_TABLE 加 {3:512} |

关键设计点：512 帧 @115200 ≈ 135ms > 静默期 ~85ms 且单缓冲无 bank
保护 → export_busy 期间禁止离开 HOLD/禁止 force_rearm，防影子 RAM
撕裂（三块 RAM 本就是 512 深，零新增 M9K）。

验证：新建 tb_wave_capture512（sim/batch_run_cap512.do）——512 帧格式
逐字节比对、预触发 64 起点、窗口跨 511→0 回绕、导出锁 HOLD 四项
全验 PASS；旧模式回归（batch_run_capture/chain/p2）全 PASS。

### 19.20 RTL 动工第二步：卷积腿系数与金标向量导出（2026-07-31）

tools/export_conv_rom.py（n5b）：全部计算直接 import
pipeline_board/wiener_conv_fix/fz_fix 函数本体，与 524 批盲跑口径
零分叉。产物：

1. **rtl/conv_rom_init.vh**（563 行，自动生成勿手改）：
   - k_rom[330] int16：维纳核时域窗一维化，Kmat[n,m]=k_rom[n-m+219]，
     位真重建断言与 Kq 全等；ksh=24，值域 [-16462, 30515]
   - hann_rom[220] 17bit 无符号（HAN=hanning(220)×2，峰值 65533，
     注意超 16 位）；xq=(w220×hann)>>>15
   - localparam：CONV_KSH=24 / CONV_DQ4=+26（307 棒自标，与盲跑
     一致）/ CONV_THR_DIR=-6650 / CONV_THR_FZ=-6894 /
     CONV_LAG307_Q4=449 / CONV_MIN_WIEN=10；d=(lag16·175+128)>>8
2. **tmp/conv_tb/ 金标向量**（供 n5c RTL 对拍）：
   - 123 帧 = 6 代表批×20 锤（g07/g15/g16/g11/g12/g19，甜区组取首个
     route-ok 批）+ 3 个 prep_fix 拒绝帧（valid=0 路径）
   - frame_%03d.mem / all_frames.mem：512 点×24bit 原始帧（与影子
     RAM wr_raw 通道一致）
   - golden_eq.mem：逐帧 e_q[111] 40bit hex（无效帧填 0）
   - golden_meta.txt：valid/lag/amp票/fz有效/fz值（fz 腿独立于卷积
     有效性，震铃锤 fz 值照记）
   - expected_batch.txt：route/med2/lag16/d 近远/amp 票/fz 票；
     eacc_*.mem：归一累加包络（RTL 串行除法对拍）

批级抽样自检（与 pipeline_board 盲跑同数）：g15 b0 d=353（真值
307，该批本就 46mm 离群批）、g16 b0 d=311、g11 b0 d_far=689、
g12 b0 d_far=696（真值 695）、g07 route=0 amp 票 20/20 好、
g19 route=0 走 fb（裙边口径）。

### 19.21 RTL 动工第三步：卷积腿模块 defect_conv_locator（2026-07-31）

rtl/defect_conv_locator.v（n5c，862 行，编译零错零警）：与
pipeline_board 定点链逐位一致的连续测距腿，接口对齐 dfc（镜像写
口 + capture_done 启动 + rearm/batch_clear）。结构：

1. **帧级 26 态状态机**：基线(Σ24·2731>>>16) → 全帧扫描（冲击峰
   p0/pk0 + 全帧 argmax/极性）→ 三分类门（弱敲 pk0<50000∨p0<16
   不占槽；震铃锤窗越帧占槽卷积无效；有效进卷积）→ xq=加窗定标
   (3拍/点×220) → 串行 MAC e[n]=|Σxq·k|（2拍/项×220×111）→
   amp 票 + lag + 插入排序 slag[20] → 归一累加
   Eacc+=(e<<15)/emax（51 步共享恢复除法×111）
2. **批级自动评估**（第 20 槽帧后连跑）：med2/q25_4/q75_4 全组合
   逻辑（np.percentile Q2/Q4 整数精确插值）→ frac 计数 → route
   → 甜区窗 Eacc argmax → 整数抛物线 off16（rdiv 幅值除法+符号
   回贴，负分子 floor 等价：q=-((|num|+b/2+b-1)/b)）→
   lag16=16i+off16-DQ4 → d=(lag16·175+128)>>8、d_far=L-d
3. **资源**：共享 33×33 乘法器 1 个 + 51 步串行除法器 1 个；
   RAM mirror 512×24 / xq 256×16 / e 128×36（理论上界 2^35 实测
   34bit）/ Eacc 128×20（≤20·32768）；每帧 ~57k 拍 @40MHz ≈1.4ms

仿真（sim/tb_defect_conv_locator.v + batch_run_conv.do）一次全绿：

- 123 帧帧级 slot/conv_valid/amp票/lag + e_ram 111 点逐位对拍
- 6 批批级 route/nvalid/av_cnt/med2/lag16/d 与金标逐位一致，
  route-ok 批 Eacc 111 点逐位对拍（g15/16/11/12），d_far 自洽
- 尾部 3 震铃帧占槽（slot_cnt=3）、全零弱敲 invalid 不占槽、
  batch_clear 清 slot/nvalid/Eacc（clr_pend→ST_CLR 延迟清零）

金标口径备忘：gold_batch route=0 批的 med2/lag16/d 字段为 0，TB
仅在 route=1 时比对（RTL 保留原料值 nvalid/av_cnt 恒比）；123 帧
全部占槽（尾部 3 帧是震铃锤而非弱敲，正好覆盖窗越帧路径）。

下一步 n5d：判决集成——fz 方向票需 47bin Goertzel RTL（fz_fix.py
位真模型，dfc 有 13bin 基础设施可扩），lag 路由 + THR_DIR 方向票
替换 LDA 档位输出挂顶层；n6 Quartus 编译评估 LE（可删未挂顶层的
defect_analyzer ~3719LE 换余量）。

### 19.22 RTL 动工第四步：fz 方向票并入卷积腿（n5d 前半，2026-07-31）

fz 腿不做独立模块，直接并入 defect_conv_locator（现 1004 行，31 态
正好占满 5bit dbg_state），复用镜像 RAM/基线/冲击峰 p0·pk0/共享
33×33 乘法器，省一套 M9K+乘法器：

1. **数据通路**（fz_fix.py 位真）：ST_FSEG（3拍/点×110，
   seg=x[p0:p0+110]>>sh_fz 无极性统一，sh_fz=bl(pk0)-14 钳≥0，
   segw=(·hann110_q15)>>>15 写回拓宽至 24bit 的 xq_ram 分时复用）
   → ST_FGOE（2拍/步×512，s0=x+((cq·s1)>>>14)-s2，s 33bit，换
   bin 首拍 fzc_q 延迟由 s1=0 吸收）→ ST_FPOW（6拍/bin，
   pw=s1²+s2²-((cq·s1)>>>14)·s2 per-bin 钳≥0，psum/hisum/losum
   64bit=numpy int64 语义，12 低带+35 高带）×47bin → ST_FLOG
   （3拍，dfc 同款前导1+FRAC=5 查表，hisum/losum 钳≥1）→
   ST_FVAL（val=5·(l2hi-l2lo)-9·l2pk，计票）
2. **流程接缝**：占槽帧均跑 fz（震铃锤 ST_CHK→FSEG；卷积帧
   ST_NACC 末 j→FSEG）；弱敲 INVF 不跑（fz_ok=0）。每帧增
   ~48.6k 拍，共 ~106k 拍 ≈2.7ms @40MHz
3. **新端口**：帧级 fz_ok/fz_val[15:0]；批级 nok_o/ndir_o/nfz_o
   + near_dir_o/near_fz_o（ST_ROUTE 锁存，near=nok≠0 且
   2·n>nok 严格大于，board_batch 同式；DIR 甜区阈/FZ fb阈）；
   计数器在复位/batch_clear/ST_BDONE 三处清零

仿真一次全绿（金标 export_conv_rom.py 扩展：gold_fz.mem 123 帧
[16]=fzok [15:0]=fzval；gold_bfz.mem 6 批 nok/nnear_dir/nnear_fz，
keep 帧直接用批量 VQ/VOK 同源值，3 拒绝帧单独跟 fz_fix_group）：

- 123 帧 {fz_ok,fz_val} 逐位对拍全过（含尾部 3 震铃帧）
- 6 批 fz 票 20/0/2・20/17/20・20/19/20・20/1/20・20/0/18・
  20/0/0 与金标一致；near_dir/near_fz 重算式对拍一致
- 全零弱敲帧 fz_ok=0；清批后 nok_c 归零

下一步 n5d 后半：顶层替换——vga_dual_mode_top 挂
defect_conv_locator，用 route+d+方向票替换 LDA 档位输出（显示/
串口口径同步改）；n6 Quartus 编译评估 LE（可删未挂顶层的
defect_analyzer ~3719LE 换余量）。

---

## 20. 算法全账：成功法 / 失败法 / 成功率 / 推导与证明

> 本节是全项目算法的统一台账，写给答辩、评测复核与后续接手者。
> 目的：把散落在 §2、§3（重构方案）、§19 各小节和实验报告里的算法
> **来龙去脉、数学推导、为什么这么写、实测成功率**收拢到一处，并
> **重点整理失败算法**（附失败原因与实测数字），避免后人重踩。
> 数据口径：铁头 9 组（07/08 好棒、11/12=695、15/16=307、19/20=800、
> 22=200），96kHz、每锤 512 点（早期 256 点）原始 ADC；除特别注明，
> 成功率均为**留一组出/盲测口径**（敲一次记一次分），非手挑子集。

### 20.1 阅读指南与全景结论

三条并行的技术路线，按“是否上板 / 是否连续物理计算”定位：

| 路线 | 输出 | 是否连续物理计算 | 是否上板 | 定位 |
|---|---|---|---|---|
| 维纳反卷积甜区腿 | 连续毫米 | ✅ 是 | ✅ 时域化免 FFT 后上板 | **测距主线** |
| fz 频带能量比方向票 | NEAR/FAR | ✅ 判向物理量 | ✅ 上板 | 甜区定侧 + 好坏辅助 |
| 频域 LDA 档位分类 | 5 档位值 | ❌ 分类贴标签 | 离线/辅助 | 兜底（板上禁作距离输出） |

**一句话结论**：缺陷落在**甜区（实证 ≈300–700mm）**且路由成功时，
板上定点链连续测距 **±20mm 命中 96%（err 中位 4–6mm）**；缺陷落在
**裙边（<~200mm / >~800mm）**时，回波与主震荡物理重叠，**连续毫米
反解在本硬件带宽下不成立**（§20.4 三法证伪），只能诚实报方向。
**唯一物理解锁是换更硬更小的锤头把冲击脉冲变窄**，软件无法补回。

### 20.2 物理模型与推导基础（为什么这么算）

#### 20.2.1 低应变反射波法基本方程

锤击在棒顶注入一个应力脉冲 `s(t)`，沿棒以纵波速 `c` 传播，遇到
阻抗突变面（缺陷截面、棒底自由端）产生反射。到达时刻 `t` 与反射
面距顶距离 `x` 满足往返关系：

```text
t = 2x / c   ⟹   x = c·t / 2 = c·(lag / FS) / 2
```

其中 lag 为相对冲击峰的采样点差，FS=96kHz。这就是所有“时域定时”
测距的根方程；一切成败都围绕“能否把某个反射的 lag 干净地读出来”。

#### 20.2.2 子腔共振与频域档位的物理基础

缺陷把全长 L 的棒切成 `d` 与 `L−d` 两段，每段像一根一端固定、一端
自由（或半自由）的短棒，形成纵向驻波共振：

```text
f = c / (2·段长)
```

故一次敲击的频谱上会出现由两段共振叠成的梳状调制，**频率位置与
缺陷位置一一对应**——这是频域 LDA 档位分类能成立的物理根。铁头脉冲
带宽宽（FWHM≈42~69 采样点，比塑料头 62~68 更硬），能激起更高的
子腔共振，故**档位分类锁定铁头协议**（跨锤头泛化实验 §20.4 已证伪）。

#### 20.2.3 镜像简并的数学证明（单端敲不可分近/远）

单端单次测量中，缺陷短腔的往返周期只反映**缺陷到最近棒端的距离**
`min(d, L−d)`：反卷积峰位 307→lag≈28(≈306mm)、695→lag≈29(≈306mm，
=1000−695 的镜像) 实测吻合。即数学上 307 与 695 的单端波形
**简并**，`f = c/(2d)` 与 `f = c/(2(L−d))` 在“取近端”意义下无法区分。

**推论**：`d` 与 `L−d` 天然关于 L/2 镜像对称，**单端单次无法定侧**
（“200↔800、307↔695 互混”的物理根源，非算法缺陷）。消歧只能靠
①时域二次回波（远端缺陷额外有一条棒底组合回波）②两端敲差分
③fz 频带能量比方向票（甜区内可用，见 §20.3-C）。

#### 20.2.4 盲区 = 冲击脉冲宽度（关键约束）

实测冲击脉冲宽度（主峰包络降到 10% 的跨度，每组 500 锤一致）：
**≈69 lag ≈ 720μs**。对应盲区距离：

```text
盲区 = c·(脉宽/FS)/2 ≈ 2100·(69/96000)/2 ≈ 680mm（半程）
```

缺陷回波理论 lag=`2d/c`：200mm→lag20、307mm→lag31 深埋脉冲内；
695mm→lag70 卡脉冲边缘；800mm→lag81 刚出脉冲但并入棒底反射。
**这解释了为何近端 200/307 时域测不到、远端 695/800 勉强可测**——
非算法问题，是脉宽物理约束。**软件无法补回，唯一解锁是窄脉冲锤头**
（脉宽 720μs→~200μs，盲区 680mm→~190mm）。

#### 20.2.5 波速实测标定 c≈2100 m/s

取代早期拍脑袋的 1900：695 甜区回波 62~63 lag 反推 2117；好棒棒底
回波 94 lag 反推 2043，两独立锚点交叉验证，取 **c≈2100 m/s**
（1 lag ≈ 10.94mm；早期文档的 8.93mm/lag 系 200 点旧口径）。

### 20.3 成功算法清单（原理 + 推导 + 为何这么写 + 成功率）

#### 20.3-A 维纳反卷积好坏判别（amp 判据，已上板）

**原理与推导**：测量信号 `x = s ∗ h`（锤脉冲 s 卷积棒冲激响应 h）。
用好棒作参考通道把锤脉冲**除掉**，复数谱维纳反卷积：

```text
Hrel(f) = X(f)·conj(T(f)) / (|T(f)|² + ε·max|T|²)   ε=0.1
h(t)   = ifft(Hrel)
判据   amp = max|h[12..110]| / max|h[0..8]|
```

模板 T = 好棒07 偶数锤平均（Hanning 窗，TL=220，PAD=1024，96kHz）。

**为何这么写**：好棒反卷积后主瓣被压成 δ 峰、晚期干净（amp≪1）；
缺陷棒短腔回波在 h[12..110] 抬头（amp↑1）。用“晚期/早期峰比”
而非绝对幅值，自动归一掉敲击力度。

**成功率**（剔除 good08 脏窗后逐锤全量，阈 amp>0.70）：

| 组 | amp p25/p50/p75 | 判定 |
|---|---|---|
| good07/08 | 0.37/0.45/0.55 | 误报 ~1% |
| 200mm | 0.67/0.75/0.87 | 检出 ~64% |
| 307mm | 0.93/1.40/1.61 | 检出 ~78% |
| 695mm | 1.41/1.66/1.84 | 检出 ~78% |
| 800mm | 0.76/0.87/0.92 | 检出 ~75~80%（旧死结打通） |

**上板化突破（免 FFT，§19.14）**：`K(f)=conj(T)/(S+λ)` 由赛前固化
模板决定，`k=irfft(K)` 是**固定实系数**；判决只用 e[0..110]，故只需
330 个 int16 ROM 系数 + 串行 MAC 111×220≈24.4k 拍 @40MHz ≈0.6ms。
定点位真：lag 一致率 99.97%（7864/7866 锤）、amp 好坏票一致率 99.99%。

#### 20.3-B 维纳反卷积甜区连续测距（测距主线，已上板）

**链路**：定点卷积腿 amp 票 + fwhm/hig 好坏判 → 定点甜区路由（批
平均包络整数归一 + 整数抛物线亚采样 Q4 + δ 定点标定）→ 连续毫米。

**测距公式（定点）**：

```text
lag16 = 16·i + off16 − DQ4          (off16 = 抛物线亚采样偏移，Q4)
d = (lag16·175 + 128) >> 8         (≈ lag16×10.94mm，175/256≈10.94/16)
d_far = L − d                      (镜像侧，由方向票定取哪个)
```

**抛物线亚采样推导**（三点 Ym/Y0/Yp）：

```text
delta = (Ym − Yp) / (2·(Ym − 2Y0 + Yp))， 限 −0.5≤delta≤0.5
```

RTL 用整数 rdiv 幅值除法 + 符号回贴（负分子 floor 等价：
q=−((|num|+b/2+b−1)/b)）。

**为何需 δ 标定**：实测反卷积峰存在 **+1.74 lag（+19mm）系统正偏置**
（包络峰延迟 + 波速误差两因子，15/16/11/12 四组一致 +0.9~+2.9）。
**修法**：赛前用自有标定棒实测一次 δ，甜区峰减 δ 再换算。LOGO 盲验
（diag_dir_logo）证明标定流程可迁移非偷看：折 A 盲测方向 100%、±20=99%。

**成功率（板上定点，THR_DIR=-6650，524 批留组盲测）**：

| 口径 | 板上定点 | v5 浮点对照 |
|---|---|---|
| 甜区路由成功批 ±20mm | **206/215 = 96%**（err 中位 4~6mm） | — |
| 甜区组(307/695) ±20mm | 206/250 = 82%（塔批诚实记0） | 169/250 = 68% |
| g11/12(695) err 中位 | 4~5mm | 5~6mm |
| g16(307) err 中位 | 6mm | 9mm |

**关键事实**：定点链全面**超过**浮点 v5（方向阈修正只进了定点链）；
甜区实证锚点只有 307/695（含镜像 693），真实下限落 200~307mm 之间
未标定，不再宣称名义窗 153~847。

#### 20.3-C fz 频带能量比方向票（已上板，位真 99.49%）

**物理直觉（用户肉眼看图发现）**：近端缺陷在冲击后 1.1ms 内有细碎
高频毛刺，远端光滑。定量化为早期窗 110 点的高/低频带能量比：

```text
fe = log10( E[2.5-9kHz] / E[0.2-2.5kHz] ) @早期110点窗
```

**力归一推导**：fe 与敲击力强相关（r≈+0.5，good 斜率 k=1.80/decade），
故 `fz = fe − 1.80×(log10(pk)−6.7)`（扣除力度斜率后的纯地信号）。

**定点 val 公式推导（fz_fix.py 位真，47bin Goertzel）**：频谱分 12 低带 +
35 高带，取 log2（前导1+FRAC=5 查表）得 l2lo/l2hi，l2pk=log2(峰能）：

```text
val = 5·(l2hi − l2lo) − 9·l2pk
```

系数 5/9 将“高低带差”与“峰能归一”在对数域线性组合，等价浮点力归一
阈；RTL 用移位加（5·x={x,2'b0}+x；9·x={x,3'b0}+x）免乘法。

**成功率（E5 新口径，塔裙边家族内方向性）**：
- 800 家族 **112/112 全对（FAR）**；307 家族 53/56 对（NEAR）；
- 板上 THR_DIR=-6650 方向 **98.1%**，甜区批 oracle 上限 206/215；
- **边界**：200 家族 1/18（塑 200 全反）——fz **不能当 200 的定向依据**，
  只能做 800/307 方向确认 + 好坏辅助。

**为何方向阈是 -6650 而非旧值 -6569**：旧阈恰好压在 NEAR 分布中心，
逐锤票在半数线乱晃；移进 NEAR/FAR 间隙（批 vmed g15/16=-6576/-6559
vs g11/12=-6724/-6827，中点 -6650）后方向 98.1%、甜区批 ±20=96%打满 oracle。

#### 20.3-D 频域 LDA 档位分类（冻结 V2，兜底腿，板上禁作距离输出）

**链路**：200点速度帧（峰对齐）→ FIR 5kHz 低通（63阶 hamming sinc）
→ hanning + rFFT256 取 bin1..13（375~4875Hz）→ log10 去均→ 16 维特征
（13谱bin + w50(FWHM) + w30 + lowd）→ z-score → LDA 五类马氏距最近类
→ k 锤多数投票。

**为何选 5kHz 与 16 维**：网格穷扫（28 组对照）定论 **0~5kHz 就是最优解**：
5kHz 压在判别频段上限（~4.1kHz 子腔共振）与接触噪声下界（~5.6kHz）
之间的天然分界，与锤头无关；>5kHz 的 bin 滤后只剩阻带地板，砂掉不掉分
还省 3 倍计算。上板形式：判决展开为线性式 `score_c = w_c·x + b_c`
（w_c=Σ⁻¹μ_c，z-score 常数折入权重），片上只做 5×16 乘累加 + argmax。

**成功率（留一组出 = 模拟赛场换场次，9 锤投票）：96.7%~100%**：

| 组（真类） | 逐帧 | 9锤投票 |
|---|---|---|
| 07/08 (good) | 90.4/81.6 | 100.0/99.5 |
| 11/12 (695) | 96.6/93.1 | 100.0/100.0 |
| 15/16 (307) | 66.4/75.8 | 96.7/98.8 |
| 19/20 (800) | 90.6/89.0 | 100.0/99.9 |

FPGA 定点纯投票口径（pipeline_fpga.py，524 批）：±15 = **476/524 (90.8%)**。

**诚实声明（为何不当主线）**：单腿 90.8% 高于双腿 v6 的 82.6%，是因
本数据集真值恰落在档位上（档对即 err=0）；赛场缺陷位置任意时，档位
输出只是最近档近似（档间隔最大 388mm）。故**用户硬规则：板上绝对
禁止档位近似输出**，LDA 仅作离线兜底/好坏辅助。

#### 20.3-E 好坏判决 OR 补漏票（两次踩坑后的正确姿势）

**设计**：`amp>0.70 或 FWHM<39.5 或 高频比>−38.6dB` 任一越阈即 DEFECT，
阈值取干净好棒批（amp≤0.70 的 07/08 批）包络 min/max 外加裕量。

**为何是 OR 而非否决**：否决票方向错——800 组 FWHM 51~57 长得像好棒
会被否决票误杀；OR 补漏只捣回 amp 漏掉的（307/200），好棒误报不涨。
**坑**：辅助特征必用 np_cache 同款 FPGA 定点速度链（改原始加速度域/
自写浮点积分 FWHM 完全无区分度）。

**成功率**：检出 +14 批（捣回 amp 漏的 307/200），好棒误报不涨；
板上链检出 392/400 → 396/400 (99%)，好棒误报 19/124 (15.3%)。

#### 20.3-F 波速实测标定 & 20.3-G 512 点采集链

**波速标定**（§20.2.5）：双锚点交叉验证 c≈2100，取代拍脑袋 1900，
直接抬高测距精度。

**512 点采集链（§19.19，已上板）**：256 点窗下卷积腿存活率=0（全灭）
——维纳对齐峰在 230~270 点晚期共振区，w220 窗需伸到 ~475 点，
故必须 512 点帧（预触发 64，冲击峰 p0≈80）。单缓冲无 bank 保护，
导出期 export_busy 禁离 HOLD 防影子 RAM 撞裂。验证：tb_wave_capture512
四项全 PASS，旧模式回归全 PASS（零新增 M9K，三块 RAM 本就 512 深）。

### 20.4 失败算法清单（重点！附失败原因 + 实测成功率 + 教训）

> 本小节是全节重点。每一条都是真金白银试出来的弯路，写明
> “当时以为它能行 → 实测多少 → 为何不行”，后人勿重踩。

#### 20.4.1 旧工程 / 官方遗留错误

**失败 1：官方单调上升边沿计数判缺陷**
- 做法：判最近 5 点是否连续单调上升，边沿计数达 11 次判异常；
  距离=单调上升持续时间×15。
- 失败原因：固定阈值不适应力度/锤头；把主冲击回摆当反射；无采样率/
  波速/往返关系；平滑变化则单调段数变。属经验拟合，不可直搬。
- 教训：**“波包数量”只能作辅助特征，不直接入判定**。

**失败 2：短时泄漏积分 `v[n]=v[n-1]−v[n-1]/4+a[n]/8`**
- 失败原因：记忆时间只几个采样点，会改变波形相位、波包持续、主冲击
  尾振、反射到达位置、正负极性——不是低应变动测所需的加速度积分。
- 正确做法：梯形积分 `v[n]=v[n-1]+a[n]+a[n-1]` + 慢速 DC blocker
  （时常≥128 点，禁为适配某批数据缩到 4~16）。

**失败 3：把硬件触发点当 T0**
- 失败原因：触发只表“冲击足够大”，非应力波真实起始；力度/锤头变时
  门限交点会移。T0 必须采集完成后回溯主峰 + 阈交点亚采样。

**失败 4：绝对值相关 `abs(sum(template×signal))`（丢极性）**
- 失败原因：把相反极性的直接缺陷反射/截面恢复反射/棒底反射全判“相似”，
  是近远对称误判重要原因；归一化相关 |cc|/RMS 还会在安静区饱和产假峰
  （好棒也冒 1.00 “缺陷峰”）。
- 正确做法：同时保留相关幅值/符号/窗能量/归一化相关度；回波定时
  一律用幅度域 |correlate| argmax。

**失败 5：固定保护区 / 300-700 固定搜索窗 / 分位置幅值比例**
- 失败原因：固定 20/30 点不兼容金属/塑料锤（窄/宽脉冲）；300/700 系旧
  方案幅值分支，与数据集 307/695 两档被混同成“赛题重点区间”——**查证
  无出处、系臆断**；赛题对位置定损无任何范围限定（图 3/4 画了上/中/下
  三种裂缝位置），裙边缺陷是真实赛场风险。
- 正确做法：保护区由本击波包宽度 + 模板自相关旁瓣自动定；路由窗由
  lag 簇聚质量自动裁决。

**失败 6：三击一致性判缺陷 / “三击后才输出”**
- 失败原因：主冲击尾振本身确定性，连续敲也稳定出现，三次同位只能
  排除随机噪声不能证明是缺陷；且评分按 10 次逐次计分，“三击才出”直接
  丢分。
- 正确做法：每击独立即时输出 NORMAL/DEFECT/INVALID，历史只作置信度。

#### 20.4.2 破对称（定侧）失败法——镜像简并难题

**失败 7：高频带 2–5kHz 能量占比定近端（已收回）**
- 曾声称 4/4 全对；2026-07-31 用“先算谱后平均”复核推翻：**两根棒规律
  相反**（Rod1 近端 307 谐波少、Rod2 近端 200 谐波多）。
- 失败原因：旧 4/4 是仅 4 个数据点骑在噪声底上的巧合，非物理规律。
- 成功率：看似 100% 实为过拟合，盲测不成立。

**失败 8：谐波多寡破对称**——同上，两根棒多寡方向相反，无一致判据。

**失败 9：P32 峰周期绝对阈值判别（手挑子集 100%，全量证伪）**
- 做法：P32=第三大峰与第二大峰 lag 差，阈 107.9 判近/远。
- 手挑好棒偶数锤干净子集上单锤 99~100%、投票全 100%；但**全量盲跑：
  同一根 307mm 棒的 g15/g16 两组 P32 中位 106.6 vs 169.2（差 60）**，
  弱锤下 P2/P3 被二次峰抢占。绝对阈 107.9 不可用于生产。
- 仅成立定性物理图景（延迟记账）：裂缝=柔性缝，近端延迟逐圈累积
  （dP3≈2×dP2）；但 dP3/dP2 同样只在干净锤稳定，**不得作量产判据**。
- 教训：**看图找到的判据必须立即全量鲁棒性验证，尤其查“同一真值不同组
  之间是否稳定”；手挑子集的 100% 不等于可盲跑。**

**失败 10：二次回波谐波判据 h2r=e[2L]/e[L]**
- 预期 NEAR 高 FAR 低，实测反向且 g16 与 FAR 重叠，最优阈总正确率
  **56% ≈ 抛硬币**，弃。

#### 20.4.3 裙边（<~200mm / >~800mm）连续测距失败法——三法均证伪

物理前提（§20.2.4）：冲击脉冲宽 ~110 点（≈双程 1.2m），反射体距两端
<~150mm 时回波与主震荡重叠，信息在时域被压缩、在频域被模态混叠。

**失败 11：时域残差包络腿裙边测距**
- 好棒模板包络扣除后找残差峰。800 端信息存在且强聚束（g20 锤中位
lag 15、iqr=1.0），但合成注入自标定证实：**裙边段斜坡把分辨率压缩 ~4:1**
（τ 走 15 步 lag_m 只走 4 级），反查表放大噪声 4 倍。
- 成功率：盲验 **±25 命中 ≈ 0**——分辨率不足、非偏置可校、不可逆。
- 注：评分容差到 200mm（§19.17），800 端批级 err 中位 68mm 系统偏差若
  输出连续毫米仍可得分，是否启用残差腿兜底待拍板（不违禁档位硬规则）。

**失败 12：谱凹槽反解 d=c/(2Δf)**
- 批平均去趋势谱 find dip。结果 **Δf 全组趋同 ~900Hz（含好棒）**——谱梳
  被系统响应/棒底模态支配，缺陷凹槽不可提取，**死路**。

**失败 13：短腔共振峰反解 d=c/(2f₀)**
- 晚期震铃窗 2-9kHz 找突出峰。结果峰位多模态混杂批间不稳（g19/20 在
  2438/3492/6820 间跳），单 argmax 不构成可靠反解——频域信息够 LDA“分类”
  但不够“连续反解”。

**失败 14：三种合成注入测甜区边界（线性副本伪造不出真实裂缝传递函数）**
- ①纯模板自合成②真实锤 raw 域单回波注入③短腔多次往返模型，τ 全域
  lag 恒 12，与实测 g15/16 lag≈31 矛盾。sanity 揭示：真实缺陷锤回波在
  维纳域 **反超主瓣**（lag31 峰 100% vs 主瓣 70%），线性注入回波最高只
  0.2~0.5。
- 结论：真实裂缝改变的是传递函数（频散/模式转换/能量重分配），
  **线性“延迟缩放副本”伪造不出来，合成法测不出甜区边界**。

#### 20.4.4 信号处理 / 滤波 失败法

**失败 15：高通/带通滤波用于测距**
- 高通/带通会产生振铃假峰并毁掉 695 定位，**禁用于测距**；5kHz 低通对
  回波定时中性（695 位置 63[62~65] 滤前后不变）、对判别与抗噪有益，上板安全。
- 频域档位网格穷扫定论：加高频必掉分（LP 12k/18k 降到 78/77%，BP 3-16k 降
  到 67%），>8kHz 是力度相关接触噪声非距离信息。

**失败 16：否决票方向设计（好坏判决）**
- 诊断证明 g08 的 14 个误报批 amp 1.2~1.6 且 FWHM 30~36，波形真长成
  缺陷样否决不掉；而 800 组 FWHM 51~57 长得像好棒会被否决票误杀。
- 正确姿势：改 OR 补漏（§20.3-E）。

**失败 17：FWHM 用原始加速度域 / 自写浮点积分链**
- FWHM 完全无区分度（好棒/缺陷全部重叠）；必用 np_cache 同款 FPGA 定点
  速度链（window_frame→velocity→V0 对齐→冲击峰归一 200 点）。

#### 20.4.5 违规 / 工程失败

**失败 18：800 写死固定值（v4，违反硬规则，已整改）**
- v4 家族分支最后一行 `d = 200 if median(p32)>107.9 else 800`——直接
  返回字面常量 800/200，输入波形里没有任何数被换算成 800，本质是两档
  分类器贴标签，违反零训练纯物理连续计算原则。
- 整改方向：测不出连续距离时如实报“近端/远端家族，无连续毫米值”，
  绝不返回伪造固定数（§19.9 [E4] 已改 fz 方向票）。

**失败 19：档位腿单腿上板（已被用户硬规则否决）**
- 资源上可行（单腿 90.8%）但板上输出档位近似违反“距离必须连续物理
  计算”硬规则，改走维纳反卷积时域化连续测距上板（§19.14）。

### 20.5 成功率总表（流水线版本演进，524 批留组盲测口径）

各版本同数据集同口径对账（敲一次记一次分；标定纪律：E1 阈只用好棒
07/08，E3 δ 只用 307 标定棒 15/16，其余均纯盲测）：

| 指标 | v4 | v5 | v6（双腿） | 板上定点终版 |
|---|---|---|---|---|
| ±15mm | 131/524 (25%) | 266/524 (50.8%) | **422/524 (80.5%)** | — |
| ±20mm | — | — | — | **311/524** |
| ±25mm | 206/524 | 274/524 | 430/524 (82.1%) | — |
| 好棒误报 | 19/124 | 19/124 | 19/124 (15.3%) | 19/124 |
| 缺陷检出 | 367/400 | 392/400 | **396/400 (99%)** | 392/400 |
| 695 err 中位 | 23mm | 4~5mm | 4~5mm | 4~5mm |
| fb 方向票 | — | 163/178 | — | 162/178 |

分档关键数（v6 新口径 524 批）：
- **g19/20 (800)：124/125，err=0**（从样本饥饿变最稳档位）；
- g11/12 (695)：24/25 + 95/100（±25 达 25/25 + 100/100）；
- g15/16 (307)：12/25 + 62/100，最弱档（轻敲信噪比 + 两腿交界）；
- **g22 (200)：0/25**（留组无 d200 训练样本是数据缺陷，非算法缺陷；
  场次内自证 12/12 全对；xfz 交叉验证可拉到 13/25，但彻底转正需补采
  铁头 200 第二场次）。

**板上定点终版（THR_DIR=-6650，±20mm 验收口径）**：
- 甜区路由成功批 **206/215 = 96%**（err 中位 4~6mm，打满 oracle）；
- 甜区组(307/695) 206/250 = 82%（塔批诚实记0）；全 524 批 ±20 = 311；
- 定点链全面超过浮点 v5（方向阈修正只进了定点链）。

**FPGA 定点档位单腿口径（非主线，仅作对比）**：变体 C（+xfz）±15 =
487/524 (92.9%)，检出 398/400，g22 13/25——高是因真值恰落档位，不可与
连续测距直接比（§19.13 诚实口径声明）。

### 20.6 方法论教训（沉淀）

1. **手挑子集的 100% 不等于可盲跑**：看图找到的判据（fz 毛刺、P32）必须
   立即全量鲁棒性验证，尤其查“同一真值不同组之间是否稳定”。
2. **两个独立判据同窗异常 = 采集事故**，可正当剔除（good08 脏窗：
   反卷积 amp 与残差 rms 同时爆表）；单判据长尾不许剔。
3. **合成数据伪造不出真实裂缝物理**：真实缺陷改变传递函数（频散/
   模式转换），线性延迟缩放副本模型测不出边界。
4. **机械安装物理一致性优先于算法参数调优**：锤头/位置/方式与采集时
   一致；盲区、带宽由锤头硬度决定，软件无法补回。
5. **诚实上限优于伪造数字**：测不出连续距离时报方向家族，绝不返回
   写死档位（板上硬规则）。
6. **单腿高分可能是数据集巧合**：本集真值恰落档位才使 LDA 单腿看似超双
   腿，赛场任意位置时档位只是最近档近似，不可当真实能力。
7. **弱锤/脏锤鲁棒性必须入链**：弱锤会让 P2/P3 峰、维纳对齐峰跑飞，
   上板前用定点速度链准入门槛 + 维纳有效锤阈守卫。




